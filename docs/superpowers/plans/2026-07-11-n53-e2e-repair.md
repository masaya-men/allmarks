# N-53: フル e2e 58本中30本落ちの修理（回帰検出網の復旧） 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or superpowers:executing-plans。Steps use checkbox (`- [ ]`) syntax.
> **推奨モデル: 全タスク Sonnet**（機械的な移植だが、テストごとの意図を読み違えると「緑にするために検証を弱める」事故になる）。

**Goal:** master 単体で 58 本中 30 本落ちている e2e を修理し、「e2e が緑」に意味を取り戻す。**テストの検証内容は弱めない**（待ち方と seed の作法だけを直す）。

**Architecture:** s186 調査で病巣は 3 つに確定した。①**version 固定 seed**: 8 ファイルが `/board` を開いた**後**に `indexedDB.open(dbName, 9)` する — アプリは既に v16 で開いているので `VersionError`（13件の正体）。「`[data-theme-id]` を待つ」ゲートは**プリペイントの inline script**（`app/(app)/layout.tsx:16-21`・localStorage 由来）が立てるので DB open 完了の保証にならない。②**`networkidle` 待ち**: dev サーバー（HMR/チャンク配信）相手では永遠に idle にならず 30 秒でタイムアウト（20件の主犯）。③**seed の各ファイル手書きコピー**: v13 の addInitScript 系はストアだけ作って**インデックスを作らない**＝実スキーマと乖離。→ 対策: **版数を固定しない共有 seed ヘルパー**を 1 個作り（アプリがスキーマを作り終えるのをポーリングで待ってから無版数 open で書く＝**将来の DB bump にも強い**）、全ファイルをそれに寄せ、`networkidle` を「テストが実際に必要とする要素の出現待ち」に置換する。

**Tech Stack:** Playwright（config: `playwright.config.ts` — timeout 既定 30s / webServer `pnpm dev` / workers 1 / retries 0）

## 事実の索引（s186 調査で確定）

- 現行 DB: `DB_NAME='booklage-db'`・`DB_VERSION=16`（`lib/constants.ts:27,30`）。スキーマの正本は `lib/storage/indexeddb.ts` の `upgrade()`（:340-719）
- **現行ストア**: `bookmarks`(id)・`cards`(id)・`settings`(key)・`preferences`(key)・`moods`(id)・`tags`(id)。`folders` は v9 で削除済み（**作ってはいけない**）
- **現行インデックス**: bookmarks: `by-folder`(:348)・`by-date`(:349)・`by-ogp-status`(:400)・`by-tag`(multiEntry, :672)／cards: `by-folder`(:354)・`by-bookmark`(:355)／tags: `by-order`(:639-640)
- **v9 で開く 8 ファイル**（Family A・VersionError）: `board-b0` `board-b0-perf` `board-b-embeds` `destefanis-save-flow` `bookmarklet-save` `lightbox-flow` `display-mode` `triage-flow`
- **v13 addInitScript の 5 箇所**（Family B・インデックス欠落）: `board-mixed-media:12` `board-backfill:27,67` `lightbox-video-flip-regression:17` `save-iframe:129`
- **networkidle 待ちの 11 ファイル**: `board-b-11-source-hide:79` `b-11-debug-open-scale:66` `board-i-07-multi-image:86,157` `display-mode:30,41,54` `lightbox-flow:29,42` `bookmarklet-save:56` `lightbox-video-flip-regression:136` `triage-flow:39` `destefanis-save-flow:30`
- 健全な最新作法の見本: `tests/e2e/mobile-share.spec.ts:21-62`（無版数 open・初回モーダル抑止 preseed）
- **(N-45) は既に完了している**: 旧 SHARE spec 3本は commit `ac0b35da` で削除済み（TODO.md:438 の記載が古いだけ。このセッションで TODO を直す）
- `lib/board/fill-snap.ts` の旧 `fillCandidates`/`snapToFill` は**本番未使用が確定**（本番は `snapToFillAtCurrentColumns` 系のみ。`FaderColumn.tsx:4,219`）＝テストごと削除可

## Global Constraints

- **検証を弱めて緑にしない**。待ちを「要素の出現」に置換するのは可、expect の削除・緩和は不可（どうしても通らないものは Task 5 で「原因の記録」に回す）
- seed に**版数を書かない**（`indexedDB.open(dbName)` 無版数のみ）。スキーマ作成コードをテスト側に書かない（アプリに作らせる）
- 1 ファイル直すごとにそのファイル単体を実行して緑を確認 → commit（巨大 1 commit にしない）
- `rtk npx` は壊れるので素の `npx playwright test` を使う。tail は失敗リスト。ANSI を剥がしてから数える

---

### Task 1: 共有 seed ヘルパー `tests/e2e/helpers/seed-db.ts`

**Files:**
- Create: `tests/e2e/helpers/seed-db.ts`

- [ ] **Step 1: Implement**

```ts
// tests/e2e/helpers/seed-db.ts
// e2e 共通の IndexedDB seed。版数を一切固定しない:
//   1. /board を開く（アプリの initDB が現行スキーマを作る/開く）
//   2. 無版数 open でアプリが作り終えたスキーマに「接続」できるまでポーリング
//   3. readwrite でレコードを書き、reload で盤面に反映
// これにより VersionError(9)<(16) が構造的に消え、将来 DB_VERSION が上がっても
// このヘルパーは修正不要。スキーマの正本は lib/storage/indexeddb.ts だけに保つ。

import type { Page } from '@playwright/test'

export const DB_NAME = 'booklage-db'

/** アプリの現行スキーマが持つ全ストア。ポーリングの完了判定に使う（作りはしない）。 */
const REQUIRED_STORES = ['bookmarks', 'cards', 'settings', 'preferences', 'moods', 'tags'] as const

export type SeedRecord = { readonly store: string; readonly value: unknown }

/** 初回モーダル/オンボを抑止する settings/preferences の定番レコード。
 *  mobile-share.spec.ts の seedBoard が入れている値をここへ一本化する
 *  （移植時に同 spec から実値をコピーして埋めること）。 */
export function firstRunSuppressors(): readonly SeedRecord[] {
  return [
    // 例: { store: 'preferences', value: { key: 'onboarding-done', ... } },
    // ↑ 実キーは tests/e2e/mobile-share.spec.ts:21-62 からコピーして確定させる
  ]
}

export async function seedDb(page: Page, records: readonly SeedRecord[]): Promise<void> {
  await page.goto('/board')
  await page.evaluate(
    async ({ dbName, requiredStores, rows }) => {
      const openCurrent = (): Promise<IDBDatabase> =>
        new Promise((resolve, reject) => {
          const req = indexedDB.open(dbName) // 無版数 = 既存版に接続
          req.onsuccess = (): void => resolve(req.result)
          req.onerror = (): void => reject(req.error)
        })
      const deadline = Date.now() + 15_000
      let db = await openCurrent()
      // アプリが全ストアを作り終えるまで待つ。開きっぱなしはアプリ側の
      // versionchange を塞ぐので、確認したら即 close して少し待って再接続。
      while (!requiredStores.every((s: string) => db.objectStoreNames.contains(s))) {
        db.close()
        if (Date.now() > deadline) throw new Error(`app schema not ready in 15s (has: ${Array.from(db.objectStoreNames).join(',')})`)
        await new Promise((r) => setTimeout(r, 150))
        db = await openCurrent()
      }
      db.onversionchange = (): void => db.close()
      const stores = Array.from(new Set(rows.map((r: { store: string }) => r.store)))
      await new Promise<void>((resolve, reject) => {
        const tx = db.transaction(stores, 'readwrite')
        for (const r of rows) tx.objectStore(r.store).put(r.value)
        tx.oncomplete = (): void => resolve()
        tx.onerror = (): void => reject(tx.error)
        tx.onabort = (): void => reject(tx.error)
      })
      db.close()
    },
    { dbName: DB_NAME, requiredStores: REQUIRED_STORES, rows: records as { store: string; value: unknown }[] },
  )
  await page.reload()
}
```

- [ ] **Step 2: `firstRunSuppressors()` の実値を確定**

`tests/e2e/mobile-share.spec.ts:21-62` の `seedBoard` を読み、初回モーダル/オンボ抑止に書いているレコードをそのまま `firstRunSuppressors()` に写す（**推測で書かない**）。

- [ ] **Step 3: 動作確認（次タスクの1本目で実証するのでここは tsc のみ）**

```bash
rtk tsc
rtk git add tests/e2e/helpers/seed-db.ts
rtk git commit -m "test(e2e): version-agnostic shared IDB seed helper (N-53)"
```

---

### Task 2: Family A（v9 の 8 ファイル）をヘルパーに移植

**移植レシピ（全ファイル共通）:**

1. ファイル冒頭の手書き `seedBoard`/`seedOne`（`indexedDB.open(dbName, 9)` を含む関数）を削除し、`import { seedDb, firstRunSuppressors } from './helpers/seed-db'` に置換
2. 旧 seed が書いていたレコード（bookmarks/cards の形はそのまま）を `seedDb(page, [...firstRunSuppressors(), ...rows])` に渡す。**レコードの中身・件数は 1 バイトも変えない**
3. `await page.locator('[data-theme-id]').first().waitFor(…)` と `await page.waitForTimeout(500)`（偽の readiness ゲート）を削除（ヘルパーのポーリングが本物の readiness）
4. reload 後の `[data-card-id]` 等の実要素待ちは**残す**

**フルに書き下した見本（1本目・`board-b0.spec.ts`）:** 旧 `seedBoard`（:6-81）を上のレシピで置換し、単体実行:

```bash
npx playwright test tests/e2e/board-b0.spec.ts
```

Expected: 7 本全緑（s184 時点で 6 本落ちていたファイル）。**緑になったらこの 1 ファイルだけで commit**:

```bash
rtk git add tests/e2e/board-b0.spec.ts
rtk git commit -m "test(e2e): board-b0 on the shared version-agnostic seed (N-53)"
```

- [ ] board-b0.spec.ts（上の見本）
- [ ] board-b0-perf.spec.ts（seed 1000 件・同レシピ）
- [ ] board-b-embeds.spec.ts（`seedOne` も同レシピ）
- [ ] destefanis-save-flow.spec.ts
- [ ] bookmarklet-save.spec.ts（open 2 箇所 :10,:32）
- [ ] lightbox-flow.spec.ts
- [ ] display-mode.spec.ts
- [ ] triage-flow.spec.ts

各ファイル: 移植 → 単体緑 → commit（`test(e2e): <file> on the shared seed (N-53)`）。

---

### Task 3: Family B（v13 addInitScript の 4 ファイル）を同ヘルパーに移植

対象: `board-mixed-media` / `board-backfill` / `lightbox-video-flip-regression` / `save-iframe`

- レシピは Task 2 と同じ。addInitScript ごと削除し `seedDb` に置換。**古い形のレコード（v12 photos-only 等）はレコードのまま入れてよい**（backfill はランタイム処理でありスキーマ版数とは無関係＝テストの意図は保たれる）
- `save-iframe.spec.ts:129` は保存**結果の検証**で open している可能性がある — その場合は読み取りだけ無版数 open に直す（seedDb は不要）。**先にテストの意図を読むこと**
- 各ファイル単体緑 → commit

- [ ] board-mixed-media.spec.ts
- [ ] board-backfill.spec.ts（:27 と :67 の 2 箇所）
- [ ] lightbox-video-flip-regression.spec.ts
- [ ] save-iframe.spec.ts

---

### Task 4: `networkidle` を実要素待ちに置換（11 ファイル）

**レシピ:** `await page.waitForLoadState('networkidle')` を削除し、**直後の操作/expect が必要とする要素**の `waitFor()` に置換する。置換先はテスト本文の次の行が触る selector（例: カードなら `[data-card-id]`、Lightbox なら開いた対象の testid）。dev サーバーでは networkidle は成立しない前提で全滅させる。

- [ ] board-b-11-source-hide.spec.ts:79
- [ ] b-11-debug-open-scale.spec.ts:66
- [ ] board-i-07-multi-image.spec.ts:86,157
- [ ] display-mode.spec.ts:30,41,54
- [ ] lightbox-flow.spec.ts:29,42
- [ ] bookmarklet-save.spec.ts:56
- [ ] lightbox-video-flip-regression.spec.ts:136
- [ ] triage-flow.spec.ts:39
- [ ] destefanis-save-flow.spec.ts:30

（Task 2/3 と同ファイルのものは同じ commit でまとめてよい）

---

### Task 5: フル実行・A/B 計測・残りの個別診断

- [ ] **Step 1: フル実行**

```bash
npx playwright test 2>&1 | sed -r "s/\x1B\[[0-9;]*[mK]//g" | tail -40
```

修理前の実測（s185）: **30 failed / 58**。目標: **failed 0**。

- [ ] **Step 2: 残った失敗の個別診断**

0 にならなかった分は 1 本ずつ: 失敗の生ログ→当該 spec の意図→アプリ側の実挙動、の順で読む（systematic-debugging）。**「アプリの実バグを検出している」可能性を必ず先に検討**してから spec を直す。直せないものは TODO.md に「ファイル名＋原因＋直し方」を書いて起票（黙って skip にしない）。

- [ ] **Step 3: flaky 1 本（29↔30 で揺れていた個体）の特定**

同じフル実行を 2 回流し、結果が揺れる spec を特定して原因をメモ（修理対象に含める）。

---

### Task 6: 掃除と記録

- [ ] `lib/board/fill-snap.ts` から `fillCandidates` / `snapToFill` を削除し、`lib/board/fill-snap.test.ts` の該当 describe（:11-119）も削除。`rtk tsc && rtk vitest run` 緑を確認
- [ ] TODO.md 更新: **N-45 は commit `ac0b35da` で完了済み**と明記して消し込み／**N-07 は本修理（VersionError 群）に吸収**として消し込み／N-53 の結果（何本→何本）を記録
- [ ] commit: `test(e2e): prune dead fill-snap exports + bookkeeping (N-53/N-45/N-07)`

## Self-Review 済みの注意点

- ポーリング open は**開きっぱなしにしない**（アプリの versionchange を塞ぐ）— ヘルパーは確認後すぐ close して再接続する設計。
- `seedDb` は `/board` を先に開く＝「アプリにスキーマを作らせる」が本質。addInitScript でアプリより先に v16 を自作する案は、インデックス・migration 正規化と乖離するので**採らない**（Family B の欠陥の再生産になる）。
- `playwright.config.ts` は触らない（timeout 30s のまま）。タイムアウトを伸ばして誤魔化さず、待ちを直す。全部直った後も遅いテストが残るなら、その時に初めて config を議論する。
