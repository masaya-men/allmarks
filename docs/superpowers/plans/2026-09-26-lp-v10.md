# LP v10 本番実装 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 承認済みの見本 v10 を、本番のトップページ(15言語)として実装する。

**Architecture:** 見本の単体 HTML/CSS/JS を、React の client section 群と `lib/marketing/lp/` の TS モジュールに分けて移植する。動きの計算で最も込み入った Features の時間割は純関数 `featState()` に切り出して単体テストで固定し、DOM への適用は section 側で行う。カードの絵は `art.ts` が1回だけ描き、その CSS は global の `lp-art.css`(`.lpRoot` の下に限定)に置く。

**Tech Stack:** Next 16.2 (App Router, `output: 'export'`) / React 19.2 / TypeScript strict / gsap 3.14 + ScrollTrigger / CSS Modules / vitest 4 / Playwright 1.59

**Spec:** `docs/superpowers/specs/2026-09-26-lp-v10-design.md`(必読)。見た目・動きの正本: `docs/private/lp-v10-mock.html`(git 管理外。行番号はこのファイルのもの)

## Global Constraints

- `any` 禁止(`unknown` + 型ガード)。関数の戻り値型は明示。tsconfig 変更禁止。
- Tailwind / Framer Motion 禁止。アニメは gsap と CSS keyframes。
- `console.log` を残さない。z-index は既存の定数か、CSS Module 内で完結する小さな値のみ(9999 等の魔法の数値禁止)。
- **Lenis を LP で使わない**(`useSmoothScroll` を LandingPage から外す。フック自体は消さない)。
- ScrollTrigger は `gsap.registerPlugin(ScrollTrigger)` をモジュール先頭で `typeof window !== 'undefined'` の時だけ実行(既存 Hero と同じ作法)。
- 各 section の `useEffect` は、作った ScrollTrigger / tween / `gsap.ticker` の関数 / IntersectionObserver / `resize` リスナ / `setInterval` をすべて cleanup で外す(dev の StrictMode 二重実行で重複しないこと)。
- スクロール中・ticker 中に寸法(getBoundingClientRect / offset* / client*)を読まない。測るのは初期化・resize・`document.fonts.ready` の時だけ。
- `prefers-reduced-motion: reduce` の時は見本の `motionOK=false` と同じ振る舞い。
- 文言は `useI18n().t('landing.…')` で取る(`@/lib/i18n/I18nProvider`)。英語固定のデザイン文字(spec §4.3)は定数。
- 見出し3キー(`hero.headline` / `problem.headline` / `cta.headline`)は `\n` を1つ含む。描画は `split('\n')`、1行で使う所は `\n` を取り除く。
- 「ボードを開く」系のリンク先は `/board`(現行と同じ)。
- 見本の Sync 区画・帯の `Sync`・料金への案内・「イントロ再生」ボタンは移植しない。
- サブエージェントは **commit しない**。各タスクの最後に「変更ファイル一覧と検証コマンドの結果」を報告して止まる。commit は司令塔が検収後に行う。
- コマンドは `rtk` を先頭に付ける(例 `rtk pnpm vitest run lib/marketing/lp`)。`--no-verify` 禁止。

## Review Focus

1. **LP から別のページへ移って戻る**(ヘッダーのリンク → ブラウザの戻る)と、section が再マウントされ、カードや動きが二重になる(`reactStrictMode` は false なので、再マウントはこの経路で起きる)→ Task 9 の e2e で、戻った後のカード枚数が初回と同じ・エラー無しを確認。ticker / ScrollTrigger / IO の外し忘れは司令塔がコードで確認。
2. **言語で見出しが長い**(de / ru / vi など)と 390px 幅で横にはみ出す → Task 9 の e2e で 15言語 × 390px の横スクロール無しを確認。
3. **文字の書体が混ざる**(日本語の文中の英字だけ別書体、Vietnamese の声調記号だけ別書体)→ Task 9 の e2e で CDP の実書体チェック。
4. **ウィンドウの大きさを変えた後**にカーソルの行き先・カードの位置がずれる → Features の resize で配置と行き先の再計測が走ること。Task 9 の e2e で 1489→390→1489 と変えた後もエラー無し・カーソルの transform に NaN が無いことを確認。
5. **動きを減らす設定**で、intro が出ない・Features が各段の完成形で止まる・動画が静止 → Task 9 の e2e を `reducedMotion: 'reduce'` でも1本走らせる。

---

## File Structure

```
lib/marketing/lp/types.ts                 (新) 共有型
lib/marketing/lp/motion-math.ts           (新) 純関数
lib/marketing/lp/motion-math.test.ts      (新)
lib/marketing/lp/masonry.ts               (新) 純関数
lib/marketing/lp/masonry.test.ts          (新)
lib/marketing/lp/feature-timeline.ts      (新) 純関数(Features の時間割)
lib/marketing/lp/feature-timeline.test.ts (新)
lib/marketing/lp/art.ts                   (新) カードと絵(DOM・client 専用)
lib/marketing/lp/marquee.ts               (新) 流れる帯(client 専用)
components/marketing/lp-art.css           (新) カード・絵の global CSS(.lpRoot 限定)
components/marketing/BackgroundGrid.tsx + .module.css (新)
components/marketing/LandingPage.tsx      (改) 構成
components/marketing/LandingPage.test.tsx (改)
components/marketing/SiteHeader.tsx + .module.css (改) ワードマーク・CTA・締めで隠す
components/marketing/sections/Hero.tsx + .module.css      (置換)
components/marketing/sections/Problem.tsx + .module.css   (置換)
components/marketing/sections/Tape.tsx + .module.css      (新)
components/marketing/sections/Features.tsx + .module.css  (置換)
components/marketing/sections/FinalCta.tsx + .module.css  (置換: 締め)
components/marketing/sections/ShareIt.tsx + .module.css   (削除)
components/marketing/LandingFonts.tsx     (新) 言語別の書体読み込み
components/marketing/landing-tokens.css   (改) 言語別の書体スタック
app/layout.tsx                            (改) Geist の subsets
messages/*.json(15)                       (改) 新キー・見出しの改行
messages/landing-linebreaks.test.ts       (新)
lib/i18n/lp-metadata.ts                   (改) `\n` を取り除く
tests/e2e/landing-v10.spec.ts             (新)
```

---

### Task 1: 文言(15言語)と見出しの改行

**Files:**
- Modify: `messages/{ar,de,en,es,fr,it,ja,ko,nl,pt,ru,th,tr,vi,zh}.json`(`landing` ブロックのみ)
- Modify: `lib/i18n/lp-metadata.ts`(`landing.hero.headline` / `landing.hero.description` を使う箇所)
- Create: `messages/landing-linebreaks.test.ts`

**Interfaces:**
- Produces: `landing.demo.{pasteHint,saved,inBrowser,noSignup,saveImage,copyLink,copied,tweet1,tweet2,tweet3}`、`landing.problem.chipList`。`landing.{hero,problem,cta}.headline` は `\n` を1つ含む。

- [ ] **Step 1: 失敗するテストを書く** — `messages/landing-linebreaks.test.ts`

```ts
import { describe, it, expect } from 'vitest'
import ar from './ar.json'
import de from './de.json'
import en from './en.json'
import es from './es.json'
import fr from './fr.json'
import itMessages from './it.json'
import ja from './ja.json'
import ko from './ko.json'
import nl from './nl.json'
import pt from './pt.json'
import ru from './ru.json'
import th from './th.json'
import tr from './tr.json'
import vi from './vi.json'
import zh from './zh.json'

const FILES: Record<string, unknown> = { ar, de, en, es, fr, it: itMessages, ja, ko, nl, pt, ru, th, tr, vi, zh }

function get(obj: unknown, path: string): unknown {
  return path.split('.').reduce<unknown>((o, k) => (typeof o === 'object' && o !== null ? (o as Record<string, unknown>)[k] : undefined), obj)
}

const HEADLINES = ['landing.hero.headline', 'landing.problem.headline', 'landing.cta.headline']
const NEW_KEYS = [
  'landing.demo.pasteHint', 'landing.demo.saved', 'landing.demo.inBrowser', 'landing.demo.noSignup',
  'landing.demo.saveImage', 'landing.demo.copyLink', 'landing.demo.copied', 'landing.problem.chipList',
  'landing.demo.tweet1', 'landing.demo.tweet2', 'landing.demo.tweet3',
]

describe.each(Object.keys(FILES))('landing copy (%s)', (lc) => {
  const m = FILES[lc]
  it.each(HEADLINES)('%s has exactly one line break and reads as one line without it', (key) => {
    const v = get(m, key)
    expect(typeof v).toBe('string')
    const s = v as string
    expect(s.split('\n').length).toBe(2)
    const joined = s.replace(/\n/g, '')
    expect(joined).toBe(joined.trim())
    expect(joined).not.toMatch(/ {2}/)
    for (const line of s.split('\n')) expect(line.trim().length).toBeGreaterThan(0)
  })
  it.each(NEW_KEYS)('%s is a non-empty string', (key) => {
    const v = get(m, key)
    expect(typeof v).toBe('string')
    expect((v as string).trim().length).toBeGreaterThan(0)
  })
  it.each(['landing.demo.tweet1', 'landing.demo.tweet2', 'landing.demo.tweet3'])('%s has two paragraphs', (key) => {
    const s = get(m, key) as string
    expect(s.split('\n\n').length).toBe(2)
  })
  it('demo labels carry no ✓ / ↓ marks (the UI adds them)', () => {
    for (const key of ['landing.demo.saved', 'landing.demo.copied', 'landing.demo.saveImage']) {
      expect(get(m, key) as string).not.toMatch(/[✓↓]/)
    }
  })
})
```

- [ ] **Step 2: 失敗を確認** — Run: `rtk pnpm vitest run messages/landing-linebreaks.test.ts` → FAIL(キーが無い・改行が無い)

- [ ] **Step 3: ja と en を入れる**(ユーザー承認済みの文面。一字一句このとおり)

ja.json の `landing`:
- `hero.headline`: `リンクを、\nビジュアルボードに。`
- `problem.headline`: `溜めるだけで、\n見返さない。`
- `cta.headline`: `ブラウザで、\nすぐに使えます。`
- `problem.chipList`: `いつもの一覧`
- `demo`: `{ "pasteHint": "リンクを貼り付けて保存", "saved": "保存しました", "inBrowser": "このブラウザに保存", "noSignup": "登録不要", "saveImage": "画像を保存", "copyLink": "リンクをコピー", "copied": "コピーしました", "tweet1": "雨の日は、窓際の席がいい。\n\n静かな喫茶店を3つ、ボードにまとめました。どこも、古いレコードが小さな音でかかっています。", "tweet2": "保存したまま忘れていたリンクを、週末にまとめて見返す。\n\n一覧だと流してしまうものも、並べると目が止まります。", "tweet3": "オーク材と白いタイルのキッチン。\n\n写真を並べてみると、好きなものの共通点が少しずつ見えてきます。" }`

en.json の `landing`:
- `hero.headline`: `Turn links into \na visual board.`
- `problem.headline`: `You pile them up \nand never look again.`
- `cta.headline`: `Works right \nin your browser.`
- `problem.chipList`: `The usual list`
- `demo`: `{ "pasteHint": "Paste a link to save", "saved": "Saved", "inBrowser": "Saved in this browser", "noSignup": "No sign-up", "saveImage": "Save image", "copyLink": "Copy link", "copied": "Copied", "tweet1": "On rainy days, I like a window seat.\n\nI put three quiet cafés on one board. All of them play old records, softly.", "tweet2": "On weekends, I look back at links I saved and forgot.\n\nIn a list I scroll past them. Laid out on a board, they catch my eye.", "tweet3": "A kitchen in oak and white tile.\n\nWhen I put the photos side by side, I slowly see what the things I like have in common." }`

- [ ] **Step 4: 残り13言語を翻訳して入れる**
  - 規則: 各言語ファイルの既存 `landing.*` を先に読み、「board」「save」「tag」などの用語を**そのファイルの既存の訳語に合わせる**。調子は落ち着いた事実だけ(感嘆符・誇張なし)。`AllMarks` は訳さない。
  - 見出し3キーは、その言語で自然な句の切れ目に `\n` を1つ。空白で区切る言語(de es fr it nl pt ru tr vi ar)は `\n` の**前に**空白を1つ残す(`\n` を除くと正しい1行になる)。zh / ko は読点・句の切れ目、th は句の間の空白の位置(空白は `\n` の前に残す)。
  - `demo.tweet1..3` は段落の区切り `\n\n` を1つ保つ。
  - `demo.saved` / `demo.copied` / `demo.saveImage` に ✓ ↓ を入れない。

- [ ] **Step 5: `lib/i18n/lp-metadata.ts` で `\n` を取り除く** — `translate(..., 'landing.hero.headline')` と、ほかに3つの見出しキーを1行として使う箇所すべて(`rtk grep -rn "landing.hero.headline\|landing.problem.headline\|landing.cta.headline" app components lib` で確認)に `.replace(/\n/g, '')` を付ける。

- [ ] **Step 6: 通ることを確認** — Run: `rtk pnpm vitest run messages` → PASS(`landing-parity.test.ts` を含む)。`rtk pnpm tsc --noEmit` → exit 0

- [ ] **Step 7: 報告して止まる**(commit しない)

---

### Task 2: 純関数(motion-math / masonry / feature-timeline)

**Files:**
- Create: `lib/marketing/lp/types.ts`, `motion-math.ts`, `masonry.ts`, `feature-timeline.ts` と各 `.test.ts`

**Interfaces:**
- Produces(後続タスクがこの名前と型で使う):
  - `types.ts`: `ArtKind`, `TweetIndex`, `CardSpec`, `Pt`
  - `motion-math.ts`: `clamp01(x)`, `eio(t)`, `E(f,a,b)`, `lerp(a,b,t)`, `Waypoint`, `wp(t, pts): Pt`, `isPress(t, tc): boolean`
  - `masonry.ts`: `Slot`, `MasonryLayout = { cw: number; slots: Record<number, Slot> }`, `masonry(specs, order, W, cols, gap): MasonryLayout`
  - `feature-timeline.ts`: `FeatureTag`, `FeatureCardSpec`, `FEATURE_CARDS`, `CARD_NEW=0`, `CARD_FILM=1`, `CARD_DRAG=2`, `CARD_SLIDES=3`, `FeatureLayouts`, `featureLayouts(W)`, `PillBox`, `FeatureGeometry`, `FeatureState`, `featState(P, g)`, `featTarget(progress)`

- [ ] **Step 1: `types.ts` を作る**

```ts
/** カードの絵の種類(見本 ART のキー)。 */
export type ArtKind =
  | 'halftone' | 'halftone3' | 'film' | 'dither' | 'truchet' | 'swiss'
  | 'ticker' | 'arches' | 'grid' | 'rules' | 'bars' | 'vinyl' | 'tag'

/** ツイート風カードの文(landing.demo.tweet1..3)。 */
export type TweetIndex = 1 | 2 | 3

/** ボード見本のカード1枚。art か tweet のどちらか。a = 幅/高さ。accent = 1 緑 / 2 橙 / 3 青。 */
export type CardSpec = {
  readonly art?: ArtKind
  readonly tweet?: TweetIndex
  readonly a: number
  readonly accent?: 1 | 2 | 3
}

export type Pt = { x: number; y: number }
```

- [ ] **Step 2: 失敗するテストを書く** — `motion-math.test.ts`

```ts
import { describe, it, expect } from 'vitest'
import { clamp01, eio, E, lerp, wp, isPress } from './motion-math'

describe('motion-math', () => {
  it('clamp01', () => { expect(clamp01(-1)).toBe(0); expect(clamp01(0.3)).toBe(0.3); expect(clamp01(2)).toBe(1) })
  it('eio is 0 / .5 / 1 at the ends and middle', () => { expect(eio(0)).toBe(0); expect(eio(0.5)).toBe(0.5); expect(eio(1)).toBe(1) })
  it('E maps [a,b] to eased 0..1 and clamps outside', () => {
    expect(E(0.1, 0.2, 0.4)).toBe(0); expect(E(0.5, 0.2, 0.4)).toBe(1); expect(E(0.3, 0.2, 0.4)).toBeCloseTo(0.5)
  })
  it('lerp', () => { expect(lerp(10, 20, 0.25)).toBe(12.5) })
  it('wp holds the first point before, the last after, and eases between', () => {
    const pts = [[0.2, 0, 0], [0.6, 100, 50]] as const
    expect(wp(0.1, pts)).toEqual({ x: 0, y: 0 })
    expect(wp(0.9, pts)).toEqual({ x: 100, y: 50 })
    expect(wp(0.4, pts)).toEqual({ x: 50, y: 25 })
  })
  it('isPress is true from tc-.012 up to tc+.03', () => {
    expect(isPress(0.627, 0.64)).toBe(false); expect(isPress(0.629, 0.64)).toBe(true)
    expect(isPress(0.669, 0.64)).toBe(true); expect(isPress(0.671, 0.64)).toBe(false)
  })
})
```

`masonry.test.ts`

```ts
import { describe, it, expect } from 'vitest'
import { masonry } from './masonry'

describe('masonry (greedy shortest column)', () => {
  it('places cards into the shortest column', () => {
    const L = masonry([{ a: 1 }, { a: 2 }, { a: 0.5 }], [0, 1, 2], 210, 2, 10)
    expect(L.cw).toBe(100)
    expect(L.slots[0]).toEqual({ x: 0, y: 0, h: 100 })
    expect(L.slots[1]).toEqual({ x: 110, y: 0, h: 50 })
    expect(L.slots[2]).toEqual({ x: 110, y: 60, h: 200 })
  })
  it('only lays out the ids in order', () => {
    const L = masonry([{ a: 1 }, { a: 1 }, { a: 1 }], [2], 100, 1, 0)
    expect(Object.keys(L.slots)).toEqual(['2'])
  })
})
```

- [ ] **Step 3: 失敗を確認** — `rtk pnpm vitest run lib/marketing/lp` → FAIL(モジュールが無い)

- [ ] **Step 4: `motion-math.ts` と `masonry.ts` を実装**

```ts
// motion-math.ts
import type { Pt } from './types'

export function clamp01(x: number): number { return x < 0 ? 0 : x > 1 ? 1 : x }
/** 見本の eio(3次の ease-in-out)。 */
export function eio(t: number): number { return t < 0.5 ? 4 * t * t * t : 1 - Math.pow(-2 * t + 2, 3) / 2 }
/** f が a→b を進む割合(ease 済み・範囲外は 0/1)。 */
export function E(f: number, a: number, b: number): number { return eio(clamp01((f - a) / (b - a))) }
export function lerp(a: number, b: number, t: number): number { return a + (b - a) * t }

export type Waypoint = readonly [t: number, x: number, y: number]
/** 時刻 t における通過点列上の位置(区間ごとに eio で補間)。 */
export function wp(t: number, pts: readonly Waypoint[]): Pt {
  const first = pts[0]
  if (t <= first[0]) return { x: first[1], y: first[2] }
  for (let k = 1; k < pts.length; k++) {
    const b = pts[k]
    if (t <= b[0]) {
      const a = pts[k - 1]
      const u = eio((t - a[0]) / (b[0] - a[0]))
      return { x: a[1] + (b[1] - a[1]) * u, y: a[2] + (b[2] - a[2]) * u }
    }
  }
  const last = pts[pts.length - 1]
  return { x: last[1], y: last[2] }
}
/** クリックの押し込み表示の窓(tc の少し前から少し後まで)。 */
export function isPress(t: number, tc: number): boolean { return t >= tc - 0.012 && t < tc + 0.03 }
```

```ts
// masonry.ts
export type Slot = { x: number; y: number; h: number }
export type MasonryLayout = { cw: number; slots: Record<number, Slot> }

/** 見本の masonry: order の順に、いちばん低い列へ積む。a = 幅/高さ。 */
export function masonry(specs: readonly { a: number }[], order: readonly number[], W: number, cols: number, gap: number): MasonryLayout {
  const cw = (W - gap * (cols - 1)) / cols
  const hs: number[] = new Array(cols).fill(0)
  const slots: Record<number, Slot> = {}
  for (const id of order) {
    const c = hs.indexOf(Math.min(...hs))
    const h = cw / specs[id].a
    slots[id] = { x: c * (cw + gap), y: hs[c], h }
    hs[c] += h + gap
  }
  return { cw, slots }
}
```

- [ ] **Step 5: `feature-timeline.test.ts` を書く**

```ts
import { describe, it, expect } from 'vitest'
import { featState, featTarget, featureLayouts, FEATURE_CARDS, CARD_FILM, type FeatureGeometry } from './feature-timeline'

const g: FeatureGeometry = {
  L: featureLayouts(700),
  bw: 700, bh: 460, bx: 16, by: 58, shotK: 0.2,
  pills: { all: { x: 8, w: 30 }, music: { x: 60, w: 44 }, design: { x: 126, w: 50 } },
  to: {
    motion: { x: 690, y: 28 }, share: { x: 610, y: 28 }, music: { x: 330, y: 28 }, design: { x: 400, y: 28 },
    start: { x: 366, y: 300 }, copy: { x: 420, y: 500 }, rest: { x: 660, y: 510 },
  },
}
/** 段 seg の中の進み f(0..1)に当たる P。 */
const at = (seg: number, f: number): number => seg + 0.08 + 0.72 * f
const idx = (tag: string): number[] => FEATURE_CARDS.map((c, i) => (c.tag === tag ? i : -1)).filter((i) => i >= 0)

describe('featureLayouts', () => {
  it('uses 3 columns at 700px and puts every card in layout C', () => {
    const L = featureLayouts(700)
    expect(L.C.cw).toBeCloseTo((700 - 20) / 3)
    expect(Object.keys(L.C.slots)).toHaveLength(FEATURE_CARDS.length)
    expect(L.A.slots[0]).toBeUndefined()
  })
})

describe('featState', () => {
  it('P<=0 is the very start: the new card is not visible yet', () => {
    const s = featState(0, g)
    expect(s.seg).toBe(0); expect(s.f).toBe(0); expect(s.cards[0].visible).toBe(false)
  })
  it('01: the paste pill is done after the card lands', () => {
    expect(featState(at(0, 0.6), g).pasteDone).toBe(true)
  })
  it('03: the film plays, MOTION is on, the film card is emphasised', () => {
    const s = featState(at(2, 0.3), g)
    expect(s.playing).toBe(true); expect(s.motionOn).toBe(true)
    expect(s.focus).toBeGreaterThan(0.99); expect(s.cards[CARD_FILM].scale).toBeCloseTo(1.03)
  })
  it('03: the cursor presses MOTION at f=.645 and a ring expands', () => {
    const s = featState(at(2, 0.645), g)
    expect(s.cursor.pressed).toBe(true); expect(s.ring).not.toBeNull()
  })
  it('03: after the click everything is stopped', () => {
    const s = featState(at(2, 0.9), g)
    expect(s.playing).toBe(false); expect(s.motionOn).toBe(false)
  })
  it('04: music first, then design only', () => {
    const m = featState(at(3, 0.45), g)
    expect(m.activeTag).toBe('music')
    for (const i of idx('music')) expect(m.cards[i].visible).toBe(true)
    for (const i of [...idx('design'), ...idx('life')]) expect(m.cards[i].visible).toBe(false)
    const d = featState(at(3, 0.95), g)
    expect(d.activeTag).toBe('design')
    for (const i of idx('design')) expect(d.cards[i].visible).toBe(true)
    for (const i of [...idx('music'), ...idx('life')]) expect(d.cards[i].visible).toBe(false)
  })
  it('05: the start screen covers the board, then wipes away after the click', () => {
    const a = featState(at(4, 0.2), g)
    expect(a.startVisible).toBe(true); expect(a.startOpacity).toBeGreaterThan(0.99)
    expect(featState(at(4, 0.4), g).startPress).toBe(true)
    const b = featState(at(4, 0.95), g)
    expect(b.startVisible).toBe(false); expect(b.frameT).toBe(1); expect(b.chipOpacity).toBeGreaterThan(0.99)
  })
  it('06: SHARE press → the board becomes an image → copied', () => {
    expect(featState(at(5, 0.2), g).sharePress).toBe(true)
    const s = featState(at(5, 0.9), g)
    expect(s.copied).toBe(true); expect(s.boardScale).toBeCloseTo(0.8); expect(s.chipsT).toBe(1)
  })
  it('the rail follows P/6', () => { expect(featState(3, g).rail).toBe(0.5) })
})

describe('featTarget', () => {
  it('maps pin progress to P with the same offset as the mock and clamps', () => {
    expect(featTarget(0)).toBe(0); expect(featTarget(1)).toBe(6); expect(featTarget(0.5)).toBeCloseTo(3.1)
  })
})
```

- [ ] **Step 6: `feature-timeline.ts` を実装**(見本 `renderFeat` 711–763 行の計算部分の忠実な移植)

```ts
import { E, clamp01, isPress, lerp, wp } from './motion-math'
import { masonry, type MasonryLayout } from './masonry'
import type { CardSpec, Pt } from './types'

export type FeatureTag = 'life' | 'music' | 'design'
export type FeatureCardSpec = CardSpec & { readonly tag: FeatureTag }

/** 機能紹介ボードのカード。順番に意味がある: 0=01で保存されて入る / 1=03で再生される動画 / 2=02でドラッグされる / 3=03で静止画が切り替わる動画。 */
export const FEATURE_CARDS: readonly FeatureCardSpec[] = [
  { tweet: 1, a: 1, tag: 'life' },
  { art: 'film', a: 16 / 9, tag: 'music' },
  { art: 'swiss', a: 3 / 4, tag: 'design' },
  { art: 'halftone3', a: 3 / 4, tag: 'design' },
  { art: 'truchet', a: 1, tag: 'design' },
  { tweet: 3, a: 1, tag: 'life' },
  { art: 'bars', a: 4 / 5, tag: 'music' },
  { art: 'tag', a: 3 / 4, tag: 'life' },
  { art: 'grid', a: 4 / 5, tag: 'design' },
  { art: 'vinyl', a: 1, tag: 'music' },
]
export const CARD_NEW = 0
export const CARD_FILM = 1
export const CARD_DRAG = 2
export const CARD_SLIDES = 3

export type FeatureLayouts = { A: MasonryLayout; B: MasonryLayout; C: MasonryLayout; D: MasonryLayout; E: MasonryLayout }

/** A=保存前 / B=保存後 / C=ドラッグ後 / D=music だけ / E=design だけ。 */
export function featureLayouts(W: number): FeatureLayouts {
  const n = W < 460 ? 2 : W < 760 ? 3 : 4
  const gap = W < 460 ? 8 : 10
  const oA = [1, 2, 3, 4, 5, 6, 7, 8, 9]
  const oB = [CARD_NEW, ...oA]
  const oC = oB.filter((i) => i !== CARD_DRAG)
  oC.splice(5, 0, CARD_DRAG)
  const only = (t: FeatureTag): number[] => oC.filter((i) => FEATURE_CARDS[i].tag === t)
  return {
    A: masonry(FEATURE_CARDS, oA, W, n, gap),
    B: masonry(FEATURE_CARDS, oB, W, n, gap),
    C: masonry(FEATURE_CARDS, oC, W, n, gap),
    D: masonry(FEATURE_CARDS, only('music'), W, n, gap),
    E: masonry(FEATURE_CARDS, only('design'), W, n, gap),
  }
}

export type PillBox = { x: number; w: number }

/** 配置の計算時だけ測る値(スクロール中は測らない)。座標はパネル内。 */
export type FeatureGeometry = {
  L: FeatureLayouts
  /** ボード(fboard)の幅・高さと、パネル内でのボード枠(fbw)の左上。 */
  bw: number
  bh: number
  bx: number
  by: number
  /** 06 で画像になる時の縮み量(広い画面 .2 / 幅 560 未満 .28)。 */
  shotK: number
  pills: { all: PillBox; music: PillBox; design: PillBox }
  to: { motion: Pt; share: Pt; music: Pt; design: Pt; start: Pt; copy: Pt; rest: Pt }
}

export type CardVisual = { x: number; y: number; scale: number; opacity: number; visible: boolean }

export type FeatureState = {
  seg: number
  f: number
  cards: CardVisual[]
  /** 02 でつまんだカードの浮き(0..1)。 */
  lift: number
  newOnTop: boolean
  /** 03 で再生中の動画カードの強調(0..1)。静止画が切り替わる動画の ▶ 印の不透明度にも使う。 */
  focus: number
  playing: boolean
  motionOn: boolean
  cursor: { x: number; y: number; opacity: number; pressed: boolean }
  ring: { x: number; y: number; opacity: number; scale: number } | null
  pastePress: boolean
  pasteDone: boolean
  pillsOpacity: number
  underline: PillBox
  activeTag: 'all' | 'music' | 'design'
  startOpacity: number
  startWipe: number
  startVisible: boolean
  startPress: boolean
  frameT: number
  frameOpacity: number
  chipOpacity: number
  flash: number
  shot: number
  boardScale: number
  chipsT: number
  copied: boolean
  sharePress: boolean
  rail: number
}

export function featState(Pin: number, g: FeatureGeometry): FeatureState {
  const P = Math.max(0, Math.min(6, Pin))
  let seg = 0
  let f = 0
  if (P > 0) { seg = Math.min(5, Math.ceil(P) - 1); f = P - seg }
  /* 段落が切り替わった直後から動き出し、最後の2割は止めて見せる */
  f = clamp01((f - 0.08) / 0.72)
  const { A, B, C, D, E: EL } = g.L
  const n = FEATURE_CARDS.length
  const pos: Pt[] = new Array<Pt>(n)
  const op: number[] = new Array<number>(n).fill(1)
  const sc: number[] = new Array<number>(n).fill(1)
  const cw = A.cw
  const R = g.to.rest
  const slot = (L: MasonryLayout, i: number): Pt => ({ x: L.slots[i].x, y: L.slots[i].y })
  const mix = (L1: MasonryLayout, L2: MasonryLayout, i: number, t: number): Pt => ({
    x: lerp(L1.slots[i].x, L2.slots[i].x, t),
    y: lerp(L1.slots[i].y, L2.slots[i].y, t),
  })
  let lift = 0, curOp = 0, clickT = -1, pillsOp = 0, frameT = 0, frameOp = 0, chipOp = 0
  let shot = 0, chipsT = 0, dim = 0, focus = 0, ua = 0, ub = 0, startOp = 0, startWipe = 0, flash = 0
  let cur: Pt | null = null
  let motionOn = seg < 2, pastePress = false, pasteDone = false, sharePress = false, startPress = false, copied = false, playing = false

  if (seg === 0) {
    const t = E(f, 0.12, 0.85)
    for (let i = 1; i < n; i++) pos[i] = mix(A, B, i, t)
    const b0 = B.slots[0]
    pos[0] = { x: lerp(g.bw / 2 - cw / 2, b0.x, t), y: lerp(-cw * 0.9, b0.y, t) }
    op[0] = E(f, 0.12, 0.3)
    sc[0] = lerp(0.55, 1, t)
    pastePress = f > 0.03 && f < 0.1
    pasteDone = f > 0.5
  } else if (seg === 1) {
    const t1 = E(f, 0.22, 0.8)
    for (let i = 0; i < n; i++) pos[i] = mix(B, C, i, t1)
    lift = E(f, 0.1, 0.2) * (1 - E(f, 0.8, 0.9))
    sc[CARD_DRAG] = 1 + 0.035 * lift
    curOp = E(f, 0, 0.08) * (1 - E(f, 0.88, 0.98))
    const c2 = pos[CARD_DRAG]
    const g1 = E(f, 0, 0.18)
    cur = { x: g.bx + lerp(g.bw * 0.92, c2.x + cw * 0.5, g1), y: g.by + lerp(g.bh * 0.92, c2.y + cw * 0.45, g1) }
  } else if (seg === 2) {
    /* 1本が本当に再生され、ほかの動画は静止画が切り替わる → カーソルが MOTION を押すと全部止まる */
    for (let i = 0; i < n; i++) pos[i] = slot(C, i)
    playing = f > 0.04 && f < 0.66
    motionOn = f < 0.66
    focus = E(f, 0.02, 0.1) * (1 - E(f, 0.7, 0.8))
    dim = 0.4 * focus
    sc[CARD_FILM] = 1 + 0.03 * focus
    curOp = E(f, 0.38, 0.44) * (1 - E(f, 0.8, 0.9))
    cur = wp(f, [[0.38, R.x, R.y], [0.6, g.to.motion.x, g.to.motion.y]])
    clickT = 0.64
  } else if (seg === 3) {
    /* カーソルが music → design を順に押す */
    pillsOp = E(f, 0, 0.1)
    curOp = E(f, 0.02, 0.08) * (1 - E(f, 0.82, 0.92))
    cur = wp(f, [[0.02, R.x, R.y], [0.16, g.to.music.x, g.to.music.y], [0.42, g.to.music.x, g.to.music.y], [0.56, g.to.design.x, g.to.design.y]])
    clickT = f < 0.4 ? 0.18 : 0.58
    ua = E(f, 0.19, 0.3)
    ub = E(f, 0.59, 0.7)
    const mo = E(f, 0.2, 0.32), mm = E(f, 0.3, 0.48), no = E(f, 0.6, 0.7), nn = E(f, 0.68, 0.88)
    for (let i = 0; i < n; i++) {
      const tg = FEATURE_CARDS[i].tag
      if (tg === 'music') { pos[i] = mix(C, D, i, mm); op[i] = 1 - no; sc[i] = 1 - 0.05 * no }
      else if (tg === 'design') { pos[i] = no > 0 ? slot(EL, i) : slot(C, i); op[i] = no > 0 ? nn : 1 - mo; sc[i] = no > 0 ? 0.95 + 0.05 * nn : 1 - 0.05 * mo }
      else { pos[i] = slot(C, i); op[i] = 1 - mo; sc[i] = 1 - 0.05 * mo }
    }
  } else if (seg === 4) {
    /* 入口の画面で「ボードを開く」を押すだけ → 登録画面なしでボードが開く → このブラウザに保存 */
    const bk = E(f, 0.04, 0.18), fi = E(f, 0.1, 0.24)
    pillsOp = 1 - E(f, 0, 0.1)
    ua = 1
    ub = 1
    for (let i = 0; i < n; i++) {
      if (FEATURE_CARDS[i].tag === 'design') pos[i] = mix(EL, C, i, bk)
      else { pos[i] = slot(C, i); op[i] = fi; sc[i] = 0.95 + 0.05 * fi }
    }
    startOp = E(f, 0, 0.12)
    startWipe = E(f, 0.44, 0.58)
    curOp = E(f, 0.14, 0.2) * (1 - E(f, 0.58, 0.68))
    cur = wp(f, [[0.14, R.x, R.y], [0.34, g.to.start.x, g.to.start.y]])
    clickT = 0.4
    startPress = isPress(f, 0.4)
    frameT = E(f, 0.6, 0.82)
    frameOp = 1
    chipOp = E(f, 0.76, 0.9)
  } else {
    /* カーソルが SHARE を押す → ボードが1枚の画像に → 「リンクをコピー」を押す */
    for (let i = 0; i < n; i++) pos[i] = slot(C, i)
    frameT = 1
    frameOp = 1 - E(f, 0, 0.1)
    chipOp = frameOp
    curOp = E(f, 0.02, 0.08) * (1 - E(f, 0.86, 0.95))
    cur = wp(f, [[0.02, R.x, R.y], [0.16, g.to.share.x, g.to.share.y], [0.5, g.to.share.x, g.to.share.y], [0.7, g.to.copy.x, g.to.copy.y]])
    clickT = f < 0.5 ? 0.2 : 0.74
    sharePress = isPress(f, 0.2)
    flash = Math.sin(Math.PI * clamp01((f - 0.22) / 0.08))
    shot = E(f, 0.24, 0.46)
    chipsT = E(f, 0.46, 0.56)
    copied = f > 0.76
  }

  const cards: CardVisual[] = FEATURE_CARDS.map((_, i) => {
    const o = op[i] * (i === CARD_FILM || i === CARD_SLIDES ? 1 : 1 - dim)
    return { x: pos[i].x, y: pos[i].y, scale: sc[i], opacity: o, visible: o >= 0.01 }
  })
  const rp = clickT >= 0 && cur ? clamp01((f - clickT) / 0.07) : 0
  const ring = cur && rp > 0 && rp < 1 ? { x: cur.x, y: cur.y, opacity: 1 - rp, scale: 0.4 + 1.3 * rp } : null
  const pa = g.pills
  return {
    seg, f, cards, lift, newOnTop: seg === 0, focus, playing, motionOn,
    cursor: { x: cur ? cur.x : 0, y: cur ? cur.y : 0, opacity: curOp, pressed: lift > 0.5 || (clickT >= 0 && isPress(f, clickT)) },
    ring, pastePress, pasteDone, pillsOpacity: pillsOp,
    underline: { x: lerp(lerp(pa.all.x, pa.music.x, ua), pa.design.x, ub), w: lerp(lerp(pa.all.w, pa.music.w, ua), pa.design.w, ub) },
    activeTag: ub >= 0.5 ? 'design' : ua >= 0.5 ? 'music' : 'all',
    startOpacity: startOp, startWipe, startVisible: startOp > 0.01 && startWipe < 0.999, startPress,
    frameT, frameOpacity: frameOp, chipOpacity: chipOp,
    flash, shot, boardScale: 1 - g.shotK * shot, chipsT, copied, sharePress,
    rail: P / 6,
  }
}

/** 固定区間の進み(0..1)→ P(0..6)。見本と同じずらし(-0.2)と伸ばし(6.6)。 */
export function featTarget(progress: number): number { return Math.max(0, Math.min(6, progress * 6.6 - 0.2)) }
```

- [ ] **Step 7: 通ることを確認** — `rtk pnpm vitest run lib/marketing/lp` → PASS / `rtk pnpm tsc --noEmit` → exit 0
- [ ] **Step 8: 報告して止まる**

---

### Task 3: カードと絵(art.ts / lp-art.css)と流れる帯(marquee.ts)

**Files:**
- Create: `lib/marketing/lp/art.ts`, `lib/marketing/lp/art.test.ts`, `lib/marketing/lp/marquee.ts`, `components/marketing/lp-art.css`

**Interfaces:**
- Consumes: `types.ts`(`ArtKind`, `CardSpec`)
- Produces:
  - `ACCENTS: Record<1|2|3, string>`(`{1:'#28F100',2:'#FF5A1F',3:'#2B4BFF'}`)と `DEFAULT_ACCENT: Record<ArtKind, 1|2|3>`(見本 531 行 `DEFC` と同じ値)
  - `type StepRunner = { add(el: HTMLElement, fn: () => void, ms: number): void; tick(ts: number): void; clear(): void }` と `createStepRunner(): StepRunner`
  - `makeCard(spec: CardSpec, opts: { amb: boolean; tweetText?: string; steps?: StepRunner }): HTMLDivElement`
  - `type FilmHandle = { draw(t: number): void; t: number; acc: number; dur: number; fill: HTMLElement; tc: HTMLElement }`、`filmOf(card: HTMLElement): FilmHandle | null`、`slidesOf(card: HTMLElement): HTMLCanvasElement[] | null`
  - `createMarquee(el: HTMLElement, inner: HTMLElement, base: number): { measure(): void; destroy(): void }`

- [ ] **Step 1: 失敗するテストを書く** — `art.test.ts`(jsdom。canvas の 2D は無いので、tweet カードと DOM の骨組みだけを確認)

```ts
import { describe, it, expect } from 'vitest'
import { makeCard, ACCENTS, DEFAULT_ACCENT } from './art'

describe('makeCard', () => {
  it('builds a tweet card with the AllMarks author and the given text (as text, not HTML)', () => {
    const el = makeCard({ tweet: 1, a: 1 }, { amb: false, tweetText: 'a <b>x</b>\n\nb' })
    expect(el.classList.contains('card')).toBe(true)
    expect(el.classList.contains('tcard')).toBe(true)
    expect(el.querySelector('.tw-name')?.textContent).toBe('AllMarks')
    expect(el.querySelector('.tw-av')?.textContent).toBe('A')
    expect(el.querySelector('.tw-body p')?.textContent).toBe('a <b>x</b>\n\nb')
    expect(el.querySelector('.tw-body b')).toBeNull()
  })
  it('builds an art card that carries its accent colour', () => {
    const el = makeCard({ art: 'grid', a: 0.8 }, { amb: false })
    const art = el.querySelector<HTMLElement>('.art')
    expect(art).not.toBeNull()
    expect(art?.style.getPropertyValue('--acc')).toBe(ACCENTS[DEFAULT_ACCENT.grid])
  })
})
```

- [ ] **Step 2: 失敗を確認** — `rtk pnpm vitest run lib/marketing/lp/art.test.ts` → FAIL

- [ ] **Step 3: `art.ts` を実装** — 見本 530–628 行(`ACCS` `DEFC` `rgb` `rgba` `cv` `step`/`steppers` `ART` `drawHT` `makeCard`)を TypeScript に移植する。変えるのは次だけ:
  - `TW` 配列は持たない。tweet カードの本文は `opts.tweetText` を `textContent` で入れる(innerHTML に文字を入れない)。作者名 `AllMarks` とアイコン文字 `A` は見本どおり固定。
  - `el.__film` / `el.__slides` は `WeakMap<HTMLElement, …>` に置き換え、`filmOf(card)` / `slidesOf(card)` で取り出す(引数は `.card` 要素。内部で `.art` を引く)。
  - グローバルの `steppers` と `step()` は `createStepRunner()` に置き換え、`ART` の amb 用の `step(el, fn, ms)` 呼び出しは `opts.steps?.add(el, fn, ms)` にする(`opts.steps` が無ければ amb の繰り返しは登録しない)。`tick(ts)` は見本 654 行の `tickSteps` の中身(各 `ms` ごとに `fn()`)。
  - `Math.random()` を使う amb の動きはそのまま。
  - 絵は見本どおり作成時に1回だけ描く(film は `draw(3)` を1回。以後の描き直しは Features 側)。
- [ ] **Step 4: `lp-art.css` を作る** — 見本 121–188 行(cards / tweet / arts、`.is-off` を含む)と、274–297 行のうちカード内部の部品(`.card.onair` `.fl-play` `.fl-ui` `.fl-ui::before` `.fl-bar` `.fl-fill` `.fl-tc` `.sl` `.sl.on` `.vbadge`)を移す。**すべてのセレクタの先頭に `.lpRoot ` を付ける**(例 `.lpRoot .card{…}`)。`@keyframes` 名は `lp-` を前置して衝突を避け、参照側も合わせる。見本の CSS 変数(`--ink` `--sans` `--mono` `--cardShadow` `--io` `--ex` など)は `.lpRoot` に定義し直す(値は見本 9–16 行のまま。`--sans` は Task 9 で言語別に差し替えるので、ここでは見本の値)。
- [ ] **Step 5: `marquee.ts` を実装** — 見本 850–858 行を移植。`createMarquee(el, inner, base)`:
  - `measure()` で `inner.scrollWidth / 2` を保持
  - `gsap.ticker` に1つ関数を登録し、`window.scrollY` の差分から速度 `v` を `v += (d - v) * 0.14` で慣らし、`|v| > .6` で向きを決め、1コマの移動量 `base + min(24, |v| * .45)`。`translate3d` を `inner` に書く。見本と同じ巻き戻し(`x <= -w` で `+= w`、`x > 0` で `-= w`)
  - `IntersectionObserver(rootMargin 60px)` で画面外は止める
  - `prefers-reduced-motion: reduce` の時は ticker を登録しない
  - `destroy()` で ticker と IO を外す
- [ ] **Step 6: 確認** — `rtk pnpm vitest run lib/marketing/lp` → PASS / `rtk pnpm tsc --noEmit` → exit 0
- [ ] **Step 7: 報告して止まる**

---

### Task 4: LP の土台(Lenis を外す・背景の縦線・lp-art.css)とヘッダー

**Files:**
- Modify: `components/marketing/LandingPage.tsx`, `components/marketing/LandingPage.test.tsx`
- Create: `components/marketing/BackgroundGrid.tsx`, `components/marketing/BackgroundGrid.module.css`
- Modify: `components/marketing/SiteHeader.tsx`, `components/marketing/SiteHeader.module.css`

**Interfaces:**
- Consumes: `components/onboarding/AllMarksMark.tsx`(`<AllMarksMark className=… />`)
- Produces: `<html data-lp-finale="1">` の間ヘッダーが隠れる CSS(Task 8 がこの属性を付け外しする)

- [ ] **Step 1: テストを書き換える** — `LandingPage.test.tsx` の `use-smooth-scroll` の mock を削除し、次を追加:

```ts
vi.mock('./BackgroundGrid', () => ({ BackgroundGrid: () => null }))

it('LP は Lenis(useSmoothScroll)を使わない', async () => {
  const src = await import('node:fs').then((fs) => fs.readFileSync('components/marketing/LandingPage.tsx', 'utf8'))
  expect(src).not.toMatch(/useSmoothScroll/)
})
```

- [ ] **Step 2: 失敗を確認** — `rtk pnpm vitest run components/marketing/LandingPage.test.tsx` → FAIL
- [ ] **Step 3: `LandingPage.tsx`** — `useSmoothScroll()` の呼び出しと import を消す(`useScrollTrigger()` と data-theme / lang の effect は残す)。`import './lp-art.css'` を追加。ルート要素(`lpRoot`)の最初の子に `<BackgroundGrid />`。ルート要素に `data-locale={locale}` を付ける(Task 9 で使う)。
- [ ] **Step 4: `BackgroundGrid`** — 見本 23–27 行の `.bggrid`(固定・12本の薄い縦線・`pointer-events:none`・内容の後ろ)。マークアップは見本 405 行付近の `<div class="bggrid" aria-hidden="true"><div class="wrap"><i></i>×12</div></div>`。`.wrap` の幅は見本 20 行の定義を module 内で再現。
- [ ] **Step 5: `SiteHeader`**
  - ロゴ: `<Link …className={styles.logo}>` の中を `<AllMarksMark className={styles.mark} />` + `<span>AllMarks</span>` にする。`.logo` は `display:inline-flex;align-items:center;gap:8px;font:700 18px var(--font-geist), system-ui, sans-serif;letter-spacing:-.045em;font-style:normal`(斜体を消す)。`.mark{width:20px;height:20px;display:block}`
  - 「ボードを開く」(`styles.openApp`): 見本 35–46 行の `.roll` と同じ「文字が上へ転がって入れ替わる」ホバー(2つの span を重ね、2つ目は `aria-hidden`)。`:focus-visible` でも同じ。
  - `:global(html[data-lp-finale='1']) .header{opacity:0;pointer-events:none}` と `.header{transition:opacity .45s}`(既存の transition と合成)
  - ナビのホバー下線: 見本 47–61 行の `.site nav a` の下線(左から伸びる)を `.navLink` に移す
- [ ] **Step 6: 確認** — `rtk pnpm vitest run components/marketing` → PASS / `rtk pnpm tsc --noEmit` → 0 / `rtk pnpm build` → 成功
- [ ] **Step 7: 報告して止まる**

---

### Task 5: ヒーロー(入口の線・見出し・動くボード・スクロールの合図)

**Files:**
- Replace: `components/marketing/sections/Hero.tsx`, `components/marketing/sections/Hero.module.css`

**Interfaces:**
- Consumes: `makeCard`, `createStepRunner`(art.ts)、`useI18n`
- 文言: `landing.hero.label` / `landing.hero.headline`(`split('\n')`)/ `landing.hero.description` / `landing.hero.ctaPrimary` / `landing.hero.ctaGhost` / `landing.demo.pasteHint` / `landing.demo.saved` / `landing.demo.tweet1` / `landing.demo.tweet2`
- 定数: `Scroll`、パネルの `AllMarks` と `Motion`

- [ ] **Step 1: マークアップと CSS を移す** — 見本 413–434 行(hero)、CSS 64–114 行と 365–369 行(intro の線)、および 370–404 行の media query のうち hero / panel / paste / hrule に関する行。`Visual Bookmark Manager` は `landing.hero.label`。見出しは行ごとに見本の `.ml > span` の構造。「使い方を見る」は現行 Hero の `handleSeeHow` と同じくボタンで、Features 区画(`id="features"`)の先頭へスクロール(reduce 時は `behavior:'auto'`)。「ボードを開く」は `/board`。貼り付けの札は `modKey` を state で持ち、マウント後に Mac/iOS なら `⌘`(初期値 `Ctrl`)。✓ は見た目側で付ける(`<span className={ok}>✓</span>{t('landing.demo.saved')}`)。
- [ ] **Step 2: 動きを移す**(`useEffect` 1つ。cleanup で全部外す)— 見本 632–655 行(HERO のカード14枚・列配置・`saveOne` の貼り付け→保存の繰り返し・IO で画面外は `is-off`)と 811–834 行(intro の線。操作 `wheel`/`keydown`/`touchstart`/`pointerdown` で即完了)。変える点:
  - `HERO` 配列の `tw:0` → `tweet: 1`、`tw:1` → `tweet: 2`(本文は `t('landing.demo.tweet1')` 等を `makeCard` の `tweetText` へ)
  - `setInterval` と `requestAnimationFrame` の `tickSteps` は cleanup で止める。`createStepRunner()` を使う
  - `heroReady`(intro が終わるまで保存の繰り返しを始めない)は ref で持つ
  - reduce の時は intro と繰り返しを出さず完成形
- [ ] **Step 3: 確認** — `rtk pnpm tsc --noEmit` → 0 / `rtk pnpm build` → 成功 / `rtk pnpm vitest run components/marketing` → PASS
- [ ] **Step 4: 報告して止まる**(司令塔が dev で見た目を確認する)

---

### Task 6: Problem と流れる帯

**Files:**
- Replace: `components/marketing/sections/Problem.tsx`, `components/marketing/sections/Problem.module.css`
- Create: `components/marketing/sections/Tape.tsx`, `components/marketing/sections/Tape.module.css`

**Interfaces:**
- Consumes: `makeCard`(art.ts)、`masonry`(masonry.ts)、`E`(motion-math)、`createMarquee`(marquee.ts)
- 文言: `landing.problem.headline`(`split('\n')`)/ `landing.problem.body` / `landing.problem.chipList` / `landing.demo.tweet1` / `landing.demo.tweet2`
- 定数: ラベル `Problem`、札の `AllMarks`、帯の語 `['Save','Arrange','Play','Tag','Share']`

- [ ] **Step 1: Problem** — 見本 435–444 行・CSS 189–212 行(区切り線 `.rule` と `.label .ln` を含む)・JS 656–679 行(一覧の行 → 線 → カード。`renderProb` と寸法キャッシュ `PG` `row._cy`)・800 行(`.prob .label .ln` の線)を移す。`PB` の `tw:0`/`tw:1` を `tweet:1`/`tweet:2` に。行のキャッシュは `WeakMap` か配列で持ち、DOM に独自プロパティを生やさない。
- [ ] **Step 2: Tape** — 見本 445 行(語を4周分並べる。**`Sync` は入れない**)・CSS 115–120 行・`createMarquee(el, inner, 0.9)`。`aria-hidden="true"`。
- [ ] **Step 3: 確認** — `rtk pnpm tsc --noEmit` → 0 / `rtk pnpm build` → 成功
- [ ] **Step 4: 報告して止まる**

---

### Task 7: Features(6段・カーソル操作・再生される動画)

**Files:**
- Replace: `components/marketing/sections/Features.tsx`, `components/marketing/sections/Features.module.css`

**Interfaces:**
- Consumes: `featState`, `featTarget`, `featureLayouts`, `FEATURE_CARDS`, `CARD_*`, `FeatureGeometry`(feature-timeline.ts)/ `makeCard`, `filmOf`, `slidesOf`(art.ts)
- 文言: `landing.features.{capture,layout,live,organize,privacy}.{title,body}` と 06 の `landing.share.headline` / `landing.share.body`、`landing.demo.{pasteHint,saved,inBrowser,noSignup,saveImage,copyLink,copied,tweet1,tweet3}`、入口画面のボタンは `landing.hero.ctaPrimary`
- 定数: `Features`、`01 — Capture` … `06 — Share`、`Share` `Motion`、`all` `music` `design`、入口画面の `AllMarks`

- [ ] **Step 1: マークアップと CSS** — 見本 447–476 行、CSS 213–297 行(カード内部の部品は Task 3 で lp-art.css 済み。ここはパネル・カーソル・札・入口画面・枠・共有の札など)と 370–404 行の該当 media query(`.fpanel .slot{top:50px}` 等の 560px 以下の2段化を含む)。`id` での参照はやめ、ref で持つ(`id="features"` だけは「使い方を見る」の飛び先として残す)。
- [ ] **Step 2: 配置の計算**(初期化・resize・`document.fonts.ready` の時だけ)— 見本 690–703 行の `fLayouts` と `fPos` を1つの関数 `measure()` にし、`FeatureGeometry` を作る: `L = featureLayouts(board.clientWidth)`、`bw/bh`=ボードの client 寸法、`bx/by`=ボード枠の offsetLeft/Top、`shotK = panel.clientWidth < 560 ? .28 : .2`、`pills`=各タグボタンの `{x: offsetLeft + 8, w: offsetWidth - 16}`、`to`=見本 `rc()` と同じ計算(`getBoundingClientRect` の差から `panel.clientLeft/Top` を引いた中心。測る間だけ共有の札とボード枠の transform を外す)、`rest = {x: panel.clientWidth*.9, y: panel.clientHeight*.96}`。カードの幅と高さ(`cw`、`cw / a`)と枠の SVG rect の寸法もここで設定。
- [ ] **Step 3: 描画の適用** — `gsap.ticker` の関数(見本 773–776 行の `featFrame`): `target = featTarget(st.progress)`、`cur += (target - cur) * (reduce ? 1 : .14)`(差が .0008 未満なら target に揃える)、変化した時だけ `apply(featState(cur, geo))`。`apply` は見本 741–762 行の DOM 書き込みを `FeatureState` の値から行う(transform / opacity / clip-path / クラスの付け外しは前回と同じなら触らない)。段の文字の切り替え `setActive(floor(cur))` と見出しの高さそろえ(`ftH`)も移す。
- [ ] **Step 4: 動画** — 見本 764–768 行の `liveTick`: `state.playing && !reduce && !document.hidden` の間だけ、`filmOf(card1)` の `t += dt/1000`、41ms ごとに `draw(t)`・進み具合の線・`0:SS / 0:24` を更新。`slidesOf(card3)` を 1300ms ごとに切り替え。ticker の `dt` は 64ms を上限に丸める。
- [ ] **Step 5: 確認** — `rtk pnpm tsc --noEmit` → 0 / `rtk pnpm vitest run lib/marketing/lp components/marketing` → PASS / `rtk pnpm build` → 成功
- [ ] **Step 6: 報告して止まる**

---

### Task 8: 締め(回る円のボタン)と構成の仕上げ

**Files:**
- Replace: `components/marketing/sections/FinalCta.tsx`, `components/marketing/sections/FinalCta.module.css`
- Delete: `components/marketing/sections/ShareIt.tsx`, `components/marketing/sections/ShareIt.module.css`
- Modify: `components/marketing/LandingPage.tsx`, `components/marketing/LandingPage.test.tsx`

**Interfaces:**
- Consumes: `createMarquee`(marquee.ts)、Task 4 のヘッダー非表示 CSS(`html[data-lp-finale='1']`)
- 文言: `landing.cta.headline`(`split('\n')`)/ `landing.cta.button` / 円の `aria-label` は `landing.hero.ctaPrimary`
- 定数: ラベル `Start`、円の文字 `OPEN THE BOARD ✦ OPEN THE BOARD ✦`、流れる輪郭文字 `AllMarks`

- [ ] **Step 1: テスト** — `LandingPage.test.tsx` に構成の順番のテストを足す(各 section の mock が描画順に `data-testid` を出す形にして、`Hero → Problem → Tape → Features → FinalCta → SiteFooter` の順、`ShareIt` が無いこと)。`./sections/ShareIt` の mock は削除。
- [ ] **Step 2: 失敗を確認** — `rtk pnpm vitest run components/marketing/LandingPage.test.tsx` → FAIL
- [ ] **Step 3: 締め** — 見本 495–510 行、CSS 320–352 行、JS 801–809 行(区画の scrub timeline)と 835–844 行(円に触れると `--big` を計算して巨大な弧)、流れる輪郭文字(`createMarquee(el, inner, .55)`)。ヘッダーを隠す部分は見本の `toggleClass` の代わりに `ScrollTrigger.create({ trigger, start:'top 60px', end:'bottom top', onToggle: (self) => { if (self.isActive) document.documentElement.setAttribute('data-lp-finale','1'); else document.documentElement.removeAttribute('data-lp-finale') } })`。cleanup で属性を必ず外す。円と文字のリンク先は `/board`。
- [ ] **Step 4: 構成** — `LandingPage.tsx`: `Hero → Problem → Tape → Features → FinalCta → SiteFooter`。`ShareIt` の import と2ファイルを削除。
- [ ] **Step 5: 確認** — `rtk pnpm vitest run components/marketing` → PASS / `rtk pnpm tsc --noEmit` → 0 / `rtk pnpm build` → 成功
- [ ] **Step 6: 報告して止まる**

---

### Task 9: 言語別の書体と e2e

**Files:**
- Create: `components/marketing/LandingFonts.tsx`
- Modify: `components/marketing/landing-tokens.css`, `components/marketing/LandingPage.tsx`, `app/layout.tsx`
- Create: `tests/e2e/landing-v10.spec.ts`

**Interfaces:**
- Consumes: `data-locale` on `.lpRoot`(Task 4)
- Produces: 各文字要素に `data-lp-text`(見出しの行・本文・見本の中の文言・カードの本文)

- [ ] **Step 1: e2e を先に書く** — `tests/e2e/landing-v10.spec.ts`(既存 `playwright.config.ts` の webServer = `pnpm dev`、baseURL `http://localhost:3000`。en は `/`、他は `/<locale>`)

```ts
import { test, expect, type Page } from '@playwright/test'

const LOCALES = ['en', 'ja', 'zh', 'ko', 'es', 'fr', 'de', 'pt', 'it', 'nl', 'tr', 'ru', 'ar', 'th', 'vi'] as const
const url = (lc: string): string => (lc === 'en' ? '/' : `/${lc}`)
const TWO_FAMILIES_OK = new Set(['th', 'ar'])

async function gotoLp(page: Page, lc: string): Promise<string[]> {
  const errors: string[] = []
  page.on('pageerror', (e) => errors.push(e.message))
  page.on('console', (m) => { if (m.type() === 'error') errors.push(m.text()) })
  await page.goto(url(lc), { waitUntil: 'networkidle' })
  await page.evaluate(() => document.fonts.ready)
  return errors
}

for (const lc of LOCALES) {
  test(`LP ${lc}: no errors, one font per text element, no horizontal scroll at 390px`, async ({ page }) => {
    await page.setViewportSize({ width: 390, height: 844 })
    const errors = await gotoLp(page, lc)
    expect(errors).toEqual([])
    const overflow = await page.evaluate(() => document.documentElement.scrollWidth - document.documentElement.clientWidth)
    expect(overflow).toBeLessThanOrEqual(0)
    const cdp = await page.context().newCDPSession(page)
    await cdp.send('DOM.enable')
    await cdp.send('CSS.enable')
    const { root } = await cdp.send('DOM.getDocument', { depth: -1 })
    const { nodeIds } = await cdp.send('DOM.querySelectorAll', { nodeId: root.nodeId, selector: '[data-lp-text]' })
    expect(nodeIds.length).toBeGreaterThan(10)
    for (const nodeId of nodeIds) {
      const { fonts } = await cdp.send('CSS.getPlatformFontsForNode', { nodeId })
      const families = [...new Set(fonts.map((f) => f.familyName))]
      if (families.length === 0) continue
      const limit = TWO_FAMILIES_OK.has(lc) ? 2 : 1
      expect(families.length, `${lc}: ${families.join(', ')}`).toBeLessThanOrEqual(limit)
    }
  })
}

test('LP en at 1489×679: the film plays in step 03 and stops after MOTION', async ({ page }) => {
  await page.setViewportSize({ width: 1489, height: 679 })
  await gotoLp(page, 'en')
  const scrollToStep = async (seg: number, f: number): Promise<void> => {
    await page.evaluate(([s, ff]) => {
      const el = document.getElementById('features')?.querySelector<HTMLElement>('[data-fpin]')
      if (!el) throw new Error('no [data-fpin]')
      const top = el.getBoundingClientRect().top + window.scrollY
      const P = s + 0.08 + 0.72 * ff
      window.scrollTo(0, top + ((P + 0.2) / 6.6) * (el.offsetHeight - window.innerHeight))
    }, [seg, f])
    await page.waitForTimeout(1400)
  }
  const film = page.locator('[data-film-card]')
  await scrollToStep(2, 0.3)
  const a = await film.screenshot()
  await page.waitForTimeout(700)
  const b = await film.screenshot()
  expect(Buffer.compare(a, b)).not.toBe(0)
  await scrollToStep(2, 0.9)
  const c = await film.screenshot()
  await page.waitForTimeout(700)
  const d = await film.screenshot()
  expect(Buffer.compare(c, d)).toBe(0)
})

test('LP reduced motion: no errors and the headline is visible immediately', async ({ browser }) => {
  const ctx = await browser.newContext({ reducedMotion: 'reduce', viewport: { width: 1489, height: 679 } })
  const page = await ctx.newPage()
  const errors = await gotoLp(page, 'ja')
  expect(errors).toEqual([])
  await expect(page.locator('h1')).toBeVisible()
  await ctx.close()
})

test('LP: leaving and coming back does not duplicate cards', async ({ page }) => {
  await page.setViewportSize({ width: 1489, height: 679 })
  const errors = await gotoLp(page, 'en')
  const count = (): Promise<number[]> => page.evaluate(() =>
    ['[data-hero-board]', '[data-problem-board]', '[data-features-board]'].map((sel) => document.querySelectorAll(`${sel} .card`).length))
  const first = await count()
  expect(first).toEqual([14, 8, 10])
  await page.locator('header a[href*="features"]').first().click()
  await page.waitForLoadState('networkidle')
  await page.goBack()
  await page.waitForLoadState('networkidle')
  await page.waitForTimeout(800)
  expect(await count()).toEqual(first)
  expect(errors).toEqual([])
})

test('LP: resizing keeps the features demo valid', async ({ page }) => {
  await page.setViewportSize({ width: 1489, height: 679 })
  const errors = await gotoLp(page, 'en')
  await page.evaluate(() => {
    const el = document.querySelector<HTMLElement>('[data-fpin]')
    if (!el) throw new Error('no [data-fpin]')
    const top = el.getBoundingClientRect().top + window.scrollY
    window.scrollTo(0, top + ((3 + 0.08 + 0.72 * 0.45 + 0.2) / 6.6) * (el.offsetHeight - window.innerHeight))
  })
  await page.waitForTimeout(1200)
  for (const w of [390, 1489]) {
    await page.setViewportSize({ width: w, height: w === 390 ? 844 : 679 })
    await page.waitForTimeout(900)
    const tf = await page.locator('[data-fcur]').evaluate((el) => (el as HTMLElement).style.transform)
    expect(tf).not.toContain('NaN')
  }
  expect(errors).toEqual([])
})

test('LP links: the finale badge and button open the board', async ({ page }) => {
  await gotoLp(page, 'en')
  const hrefs = await page.locator('[data-finale-cta] a').evaluateAll((as) => as.map((a) => a.getAttribute('href')))
  expect(hrefs.length).toBeGreaterThan(0)
  for (const h of hrefs) expect(h).toBe('/board')
})
```

  必要な目印(Task 5–8 の該当要素にこのタスクで追加する): Features の固定区間 `data-fpin` / 動画カード(`FEATURE_CARDS[1]`)の `.card` に `data-film-card` / Features のカーソル `data-fcur` / 締めのリンク群の親 `data-finale-cta` / カードを入れる3つの箱 `data-hero-board` `data-problem-board` `data-features-board` / 文字要素 `data-lp-text`。ヘッダーの Features へのリンクの href は `features` を含む(既存の `navHref(locale,'features')`)。

- [ ] **Step 2: 失敗を確認** — `rtk pnpm exec playwright test tests/e2e/landing-v10.spec.ts` → 書体チェックが FAIL する言語がある(ja 等)ことを確認
- [ ] **Step 3: 書体** — 
  - `app/layout.tsx` の `Geist` の `subsets` に `latin-ext` を追加(型が許せば `cyrillic` も)。
  - `LandingFonts.tsx`: locale に応じて Google Fonts CSS2 の `<link rel="stylesheet" precedence="default" href=…>` を1本だけ描く(React 19 が head へ移す)。ja=`Noto+Sans+JP:wght@400..800`、zh=`Noto+Sans+SC:wght@400..800`、ko=`Noto+Sans+KR:wght@400..800`、th=`Noto+Sans+Thai:wght@400..800&family=Noto+Sans:wght@400..800`、ar=`Noto+Sans+Arabic:wght@400..800&family=Noto+Sans:wght@400..800`、vi=`Noto+Sans:wght@400..800`(`&display=swap` 付き)。ru は Geist の Cyrillic で e2e が通ればリンク無し、通らなければ vi と同じ。他の言語はリンク無し。`LandingPage` から描画。
  - `landing-tokens.css`: `.lpRoot{--sans: var(--font-geist), system-ui, sans-serif}` を既定に、`.lpRoot[data-locale='ja']{--sans:'Noto Sans JP', sans-serif}`、zh `'Noto Sans SC'`、ko `'Noto Sans KR'`、th `'Noto Sans Thai','Noto Sans', sans-serif`、ar `'Noto Sans Arabic','Noto Sans', sans-serif`、vi `'Noto Sans', sans-serif`。英語固定のデザイン文字(§4.3)の要素は `font-family: var(--font-geist)` / `var(--font-geist-mono)` を明示する。
- [ ] **Step 4: 通ることを確認** — `rtk pnpm exec playwright test tests/e2e/landing-v10.spec.ts` → 全 PASS。`rtk pnpm vitest run` → PASS。`rtk pnpm build` → 成功
- [ ] **Step 5: 報告して止まる**

---

### Task 10: 最終ゲートと公開(司令塔)

- [ ] `rtk pnpm tsc --noEmit` → 0 / `rtk pnpm vitest run` → 全 PASS / `rtk pnpm exec playwright test tests/e2e/landing-v10.spec.ts` → 全 PASS / `rtk pnpm build` → 成功
- [ ] `out/index.html` と `out/ja.html` に Sync 区画の文言・料金表記が無い(`rtk grep -c "Google ドライブ\|/月" out/ja.html` → 0)
- [ ] 1489×679 と 390×844 で、ヒーロー・Problem・Features の 03〜06 の要所・締めのスクショを撮り、見本と並べて確認
- [ ] 文言の機微チェック(CLAUDE.md の commit 前自己チェック)→ commit → CLAUDE.md の固定手順で公開 → push
