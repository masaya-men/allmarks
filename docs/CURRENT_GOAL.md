# 次セッションのゴール — 同期は実用段階に到達。残るは①レート制限の再確認 ②収益化・ローンチの棚卸し

## ★s218(2026-09-22〜23)の到達点

端末間同期が「仕組みが完成している」段階から「実際にユーザーが2端末で使い、フィードバックを反映した」段階まで進んだ。詳細は`docs/TODO_COMPLETED.md`の「s218続き」節。要点:

- 近リアルタイム同期化(保存のたびに自動で同期の合図が出る)・PL-1/PL-2(公開前必須タスク)完了。
- K3の運用を実際に開始(招待リンクから鍵を発行・ユーザーが実機でPC/スマホ2台の同期接続を確認済み)。
- 同期セットアップの画面を、ユーザーの実機フィードバックを受けて全面作り直し(ガイド付きモーダル)。
- テーマが端末間で意図せず同期されてしまう実データ不具合を発見・「テーマは端末ごと独立」に修正。
- 「あと何台使えるか分からない」との指摘で、端末数表示を追加。

## ★次にやること

1. **Cloudflareレート制限の再確認**(`/claim`・`/activate`向けに設定済みだが、実際にブロックが発動するか複数回テストして確認できなかった)。時間を置いて再テストするか、Cloudflareサポートに問い合わせる。実害はない(招待コード自体の高いエントロピーが本来の防御)ため急ぎではない。
2. **収益化・ローンチ準備の棚卸し**: `docs/private/`の計画(2026-07時点の資料群、特に`2026-07-12-monetization-recheck-s187.md`)は2026-08-26(s205)以降ほぼ更新されていない。その間に同期・Private機能・K3運用まで一気に完成したため、計画そのものが古くなっている。**未着手のまま残っている主なもの**: 有料テーマ(応援のお礼、計画では2〜3本予定だが現状0本)、支援の受け皿(FANBOX/Patreon口座開設、ユーザー本人の作業)、13言語対応の残り(現状不明・要確認)、宣伝素材(お披露目用ボード・告知動画、未着手)、共有画像への`allmarks.app`ロゴ焼き込みの確認(小タスク)。次回、これらを一緒に棚卸ししてから優先順位を決める。

## 恒久ルール(継承)

- 視覚変更は`ui-design.md`「承認後」。`rtk`前置・`--no-verify`禁止・vitest/playwrightは素のnpx(`rtk npx`は既知の不具合)・Framer Motion禁止。
- 音(dotted-notebook)/紙(paper-atelier)＝バイト同一を死守。
- 機微(支援・値付け・戦略)はtrackedに書かない＝`docs/private/`。
- merge/push/deployは必ずユーザー確認後。ただしdeployは「本番で見たい」等の明示的な合図があれば即実行可。docsだけのpushはしない(次の実務pushに同梱)。
- 選択ボックス(AskUserQuestion)はデザイン判断・意思決定・調査/デバッグ中の質問には使わない。普通の会話で聞く。
- 文言(UIコピー)を新規/変更するときは、実装前に実際の英語・日本語の文面そのものを見せて確認を得る。**ユーザーが指定した具体的な値(文字列など)は、実装前にそのまま使うこと — 勝手に別の値に置き換えない**。
- IDB/vault・同期のmerge処理など不可逆な本番データに関わる変更は、実行前に必ずユーザーに事実確認する。同期のmerge処理(`lib/sync/merge.ts`)は特に慎重に(可換性=commutativityが崩れていないか要確認)。
- 大規模調査・実装はサブエージェントに委譲(司令塔は診断・設計・指示書・検収)。同一ファイルへの並行実行は避ける。

## 保留中(並行可)

- **N-78**: 画像無しツイート専用カード(見た目案の提示・承認が必要)。ユーザー方針: 収益化の仕組みに着手した後に回す。
- **N-64**: カードの＋TAGポップオーバーが再表示後に開けなくなる既存バグ(`CardsLayer.tsx`)。
- **N-63**: `BackupReminder`の表示位置がScrollMeterに被る(モック→承認後)。
- **N-65**: ECDH秘密鍵unwrap時に生バイトが一瞬JS経由(severity LOW・設計変更要)。
- さらなるテーマ/Flat磨き、C2翻訳仕上げ、同期関連文言の13言語翻訳(現状は日本語以外すべて英語のまま)。
