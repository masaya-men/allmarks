# 次セッションのゴール — Private Phase 2 の残り(拡張機能のUI配線)、着手前に必ずbrainstormingから

## ★s204 の到達点
- **Private Phase 2 サブプロジェクト2(②クイック保存面: PopOut/拡張機能/ブックマークレット)、完全に完了。`master`へmerge済(`--no-ff`)・GitHubへpush済・本番デプロイ済。ユーザー実機確認は次回(またはこのセッション末尾)。**
  - 設計を「対称鍵暗号」から「公開鍵暗号(ECDH)」へ全面ピボット。ユーザー自身の指摘(「タグ付けにパスワードは要らないはず」)がきっかけ。タグ付け(暗号化)はパスワード不要でどこからでも可能、閲覧・削除(復号)のみパスワードが必要、という理想形を実現。
  - 13タスクのplanをsubagent-driven-developmentで実装。各タスクレビュー通過。
  - **superpowers:security-review**で4件の実在する脆弱性を発見・修正: ①Privateタグ付け関数の二重実行防止漏れ(データ破壊バグ、チップ連打で再現)②nonce重複排除の抜け③ブックマークレット経由で他人のブックマークを勝手に隠せる権限過多④Privateタグの有無が漏れるオラクル。
  - **最終全ブランチレビュー(opus)**でさらに2件: ①二重実行防止のガードがトランザクションの外にあり実は不完全だった(再修正)②新しい3経路がPrivate化後に盤面へ更新を知らせておらず、開いたままの盤面に中身が残り続ける(修正)。
  - **本番の古いPhase1 Private設定(パスワード)をユーザー確認の上で安全に削除**: 中身は退避済みと確認 → EXPORTバックアップ→DevToolsで古いvault設定+古いPrivateタグをまとめて削除(ブックマーク自体は無傷)。これで新方式での再設定が汚れなくできる状態。

## ★次回ユーザーに確認してほしいこと(最優先)
1. `allmarks.app`をハードリロード
2. FilterPillの「Private」行が「未設定(SETUP)」表示になっているか
3. 新しいパスワードでPrivateを作り直せるか、PopOut/拡張機能/ブックマークレットからPrivateタグ付けができるか

## ★次点＝拡張機能自身のコンテンツスクリプトUI配線(Private Phase 2の最後のピース)
- ②のplanで意図的にスコープ外にしていた部分。`extension/`のfloating-buttonなど、拡張機能自身のUI(vanilla JS)からPrivateタグを付けられるようにする配線。着手前に必ずsuperpowers:brainstormingから(小さめの計画で切り出す想定)。
- **合わせて対応すると良い軽微な積み残し(s204で発見・記録のみ)**: `docs/TODO.md`の N-65〜N-68 を参照(生鍵のJSメモリ通過/拡張機能の再送ポンプの落とし穴/PopOut・ブックマークレットのPrivateチップに成功・失敗フィードバックが無い/保存直後のPrivate化でツイート等のメディアが欠落する)。N-66は拡張機能UI配線の**着手時に必ず一緒に直す**(直さないと再送が最大30回超発火する)。

## 新規(s204ユーザー指摘、要brainstorming)
- **Privateパスワードの再設定・変更フローが無い**: 忘れた/作り直したい場合の正規の救済手段がアプリ内に無く、今回は開発者自身がDevToolsで手動対応した。設計してから追加する価値のある機能。

## 保留中(いつでも合流可)
- **N-64**: カードの＋TAGポップオーバーが、フィルタ等で一瞬消えて再表示された後にもう一度開けなくなることがある(`CardsLayer.tsx`の既存バグ、Private機能とは無関係)。
- **N-63**: バックアップ提案(`BackupReminder`)の表示位置がScrollMeterに被るバグ。視覚変更のためモック→承認後に実装。
- ResizeHandleの当たり判定が小さいカードで＋TAGボタンを奪うバグ(s203由来、視覚変更のため承認後)。
- TODO.mdが900行超(目安200行を大幅超過)→ 古いセッションnarrativeをTODO_COMPLETED.mdへ移動する軽い掃除タスク(非ブロッキング)。
- さらなるテーマ/Flat磨き、支援まわり(非公開`docs/private/IDEAS.md`)、C2翻訳仕上げ。

## 恒久ルール(継承)
- 視覚変更は`ui-design.md`「承認後」。`rtk`前置・`--no-verify`禁止・vitest/playwrightは素の npx(`rtk npx`は既知の不具合)・Framer Motion禁止。
- 音(dotted-notebook)/紙(paper-atelier)＝バイト同一を死守。
- 機微(支援・値付け・戦略)はtrackedに書かない＝`docs/private/`。
- merge/push/deployは必ずユーザー確認後(CLAUDE.md安全ルール)。ただしdeployは「本番で見たい」等の明示的な合図があれば即実行可。
- **選択ボックス(AskUserQuestion)はデザイン判断・意思決定・調査/デバッグ中の質問には使わない。普通の会話で聞く**。
- **IDB/vaultなど不可逆な本番データに関わる変更は、実行前に必ずユーザーに事実確認する**(s204でPhase1旧vault削除の際に実践)。
