# 次セッションのゴール (= セッション 106)

## 今のゴール (1 行)

**session 105: 「保存直後タグ付け 第3段(拡張なしブックマークレット)」を実装→本番確認したが、`/save` の小窓が必ず一瞬チラつく(拡張なしの保存はブラウザ仕様上 AllMarks の窓を一度開く必要があり、チラつきゼロにできない)ため、user 判断で**第3段を丸ごと撤去**。`/save` は元の「保存して即閉じる」に復帰(本番反映済、commit でリバート)。第1段(拡張のタグ帯)・第2段(PiP タグ付け)は**無傷**。次は 公開準備 へ。**

## 開始時の動き
1. このファイル + [docs/TODO.md](./TODO.md)「現在の状態 (セッション 105)」を読む
2. **本番は `allmarks.app`**(deploy は `--project-name=allmarks --branch=master`)
3. user に「次は公開準備でいい?」を確認

## 撤去の経緯(第3段)
- 拡張なしのブックマークレット保存で、`/save` ポップアップ窓を PiP 風タグUIに変身させる機能を実装(spec/plan/サブエージェント駆動で完成、tsc 0 / 全テスト緑)。
- 本番実機で `resizeTo` は効いた・ライフサイクルもOKだったが、**全ケースで「窓が中身付きでチラッと出る」のが体験を損なう**。決定するまで暗い無地にする polish も入れたが、**窓が一瞬現れること自体はゼロにできない**(拡張なしの保存に AllMarks の窓が必須=ブラウザのストレージ分離仕様)。
- → user 「チラつき0じゃないならない方がいい」=**撤去**。`components/bookmarklet/SaveToast.{tsx,module.css}` を pre-第3段に戻し、`lib/tagger/quick-tag-apply.*` と `SaveToast.test.tsx` を削除。
- **教訓**: 拡張なし経路に「窓を見せて操作させる」UXは、OS窓フラッシュが不可避なので筋が悪い。タグ付けは拡張(第1段)/PiP(第2段)で完結させる方向が正しい。

## 次の候補
- **公開準備**: i18n 言語切替の配線(要 brainstorming)/ onboarding / LP 整備 / 拡張ストア公開素材。
- **(別タスク・保留)拡張なしでもカーソルピルを出す** — `docs/private/IDEAS.md` 記録済。これは「見た目だけ」なので窓フラッシュ問題と無関係(元ページにピルを描くだけ、保存操作はしない)。本命は本物ピル(extension CSS)を透明 iframe で重ねカーソル追従させる案(B)。着手時 brainstorming から。

## 守ること
- **本番は allmarks.app**。deploy 前 `npx wrangler whoami`、tsc + vitest 通してから。実機/本番で測ってから「動いてる」と報告
- 発明しない・本物の部品を流用。横文字を日本語応答に混ぜない。AskUserQuestion ボックス禁止。デザイン変更は提案→承認→実装
- `DB_NAME='booklage-db'`・`booklage:*`・`booklage-save` 窓名 等の不可視符号は**永久に維持**
- i18n: 新 key は 15 言語全部に同期
