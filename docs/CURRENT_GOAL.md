# 次セッションのゴール — Paddle 決済と鍵の自動発行・失効を Sandbox で設計・実装する

## ★s220(2026-09-24)の到達点

有料プランの受け皿を整備した。決済は Paddle(販売代行=Merchant of Record)に決定し、料金ページ・返金ポリシーを新設、利用規約・プライバシー・問い合わせ・FAQ を全面改訂(15言語・本番公開済み)。**Paddle 本番審査を提出済み(結果待ち・最大5〜7営業日)**。詳細は `docs/TODO_COMPLETED.md`「s220」節。

**判断の正本は `docs/private/2026-09-24-paddle-decisions.md`**(料金・返金・解約・鍵の渡し方・審査内容まで全部ここ)。調査は `docs/private/2026-09-24-subscription-payment-research.md`、公開文面は `docs/private/2026-09-24-legal-pages-copy.md`。**これらは決定済み。再調査・再決定しないこと。**

## ★次にやること(この順で)

1. **設計書を書く**(`docs/private/` か `docs/superpowers/specs/`、機微は private): Paddle.js チェックアウト → `checkout.completed` の取引ID → 自前 Function が Paddle API(GET /transactions/{id})で支払い確認 → その場で鍵を発行・表示。Paddle webhook(署名検証)で契約状態を KV に保持。アプリは同期前に1日1回状態確認(7日猶予)。既存 K3(`functions/claim.ts`・`activate.ts`・`lib/board/license-*`)との統合、無償鍵(既存の招待リンク発行分)は無期限の区別。**端末を外す仕組み**も一緒に設計(TODO「同期まわりの追加要望」)。
2. **Sandbox で実装・テスト**: Sandbox アカウントはユーザー作成済み。API キー等はユーザーに Sandbox 画面から発行してもらい、`.dev.vars` / Cloudflare Secret へ(チャットに貼らせない)。偽カードで購入→鍵表示→解約→期間末停止まで通す。**領収書メールに取引ID(txn_…)が載るか**をここで確認(載らなければ紛失時の再表示の入力値を変える)。
3. アプリ内の導線: 設定 SYNC 欄の「応援する(近日公開)」→ 料金ページへ。停止時の案内文言。**文言は実装前に日英の実文を見せて承認をもらう**。
4. プライバシーの「Paddle から受け取るもの(メールアドレス)」を実装に合わせて修正(メール送信しない設計なら削る)。
5. 審査が通ったら本番キー切替 →「申し込む」ボタンを有効化 → 本番で少額テスト。その後に LP への同期訴求(文体統一と同時)。

## ユーザー本人の作業(忘れずに声をかける)

- **Paddle 審査結果のメール確認**(追加の本人確認書類を求められたら内容を見せてもらう)。
- **グループアドレス名義で返信する Gmail 設定**(実際に問い合わせへ返信する前に必須。TODO「ユーザー本人の作業待ち」)。

## 恒久ルール(継承)

- 視覚変更は見本→承認→実装。`rtk`前置・`--no-verify`禁止・vitest/playwright は素の npx・Framer Motion 禁止。
- 公開文面は**落ち着いた書き方**(「歓迎」「お気軽に」等の呼び込み表現を避ける)。日本語の呼称は「運営者」、英語は "I"。運営者名は **Masaya Maeno(ローマ字のみ)**。漢字の氏名は絶対に載せない。
- 機微(価格戦略・PF選定理由・収益)は tracked に書かない=`docs/private/`。
- merge/push/deploy はユーザー確認後。docs だけの push はしない(次の実務 push に同梱)。
- 選択ボックス(AskUserQuestion)は使わず普通の会話で聞く。決定は1つずつ。
- 課金など不可逆・外部に出る操作(本番キー切替・実課金)は必ず事前確認。
- 大規模調査・実装はサブエージェントに委譲(司令塔は設計・指示書・検収)。同一ファイルへの並行実行は避ける。
- ユーザーは「流されずに厳しく判断して」と求めている。推奨は根拠つきで出し、ユーザー案の方が正しければ理由を添えて認める。

## 保留中(並行可)

- N-78(画像無しツイート専用カード)/N-64/N-63/N-65、Cloudflare レート制限の発動確認、同期関連文言の13言語翻訳(FAQ の画面文言引用も連動)、`/api/img` のプライバシー記載漏れ、同期ボタン常設案(見本→承認)、画面復帰時の同期反映の短縮。
