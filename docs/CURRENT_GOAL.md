# 次セッションのゴール — I-5解決(パスワード復旧キー)は完了・本番反映済。残りは①リアルタイム同期の検討 ②運用セットアップ ③PL-1/PL-2(ユーザー作業)

## ★s217(2026-09-21)の到達点 — I-5「パスワードを忘れた場合の復旧手段が無い」を解決・本番反映・ユーザー実機確認済

- **方針**: ユーザーが「パスワード復旧手段は必ず必要」と明確に決定 → 1Passwordの Emergency Kit 方式(端末内で生成する復旧キー)を採用。設計スペック `docs/superpowers/specs/2026-09-21-private-recovery-key-design.md`・実装計画 `docs/superpowers/plans/2026-09-21-private-recovery-key.md`(9タスク・subagent-driven-development・全タスクレビュー済)。
- **実装内容**: 新規vault作成時は自動発行、既存vaultは次回解錠時に**強制的に**作成させる(ユーザー追加指示「気づいた人だけでなく全員に」)。「パスワードを忘れた場合」リンク→復旧キー入力→新パスワード必須設定、の一連の流れ。復旧キーは`lib/private/crypto.ts`の`generateRecoveryKey`(31文字アルファベット・30文字・150ビット相当)。既存の暗号プリミティブを流用し新しい暗号方式は追加していない。
- **最終ブランチレビュー(opus)で5件のImportant指摘を発見・全て修正**: (I-A)復旧キー用のPBKDF2繰り返し回数がレコードに自己完結しておらず将来のパスワード側定数変更で全復旧キーが無言で失効する不具合、(I-B)端末間同期のLWWマージが発行したての復旧キーを無言で消しうる不具合(**1回目の修正が不十分と2回目のレビューで判明**→`updatedAt`が同点になるタイブレーク経路にも引き継ぎ処理を適用する形に修正し直し・3周期にわたる収束を実際にトレースして確認)、(I-C)コピー完了フィードバックの欠落+計画書から漏れていたi18nキー、(I-D→ユーザー指示でI-Fに格上げ)既存ユーザーへの案内不足→「強制作成」に発展、(I-E)エラーハンドリングの取りこぼし。
- **本番確認後のユーザーフィードバック3件、全て対応・本番反映済**:
  1. 復旧キー画面を連打で誤って閉じられる不具合 → コピーするまで「わかった」/Escape/背景クリックを全て無効化(AWS方式)。クリップボードが使えない環境(非HTTPS等)での詰み防止の安全装置も追加。
  2. パスワードのヒント欄は無意味という指摘 → 4画面全てから完全削除(`vault-store.ts`のデータ層は意図的に温存・約20箇所の呼び出し元への影響回避のため)。
  3. Chromeがパスワード更新を提案してこない → 3つの入力画面を本物の`<form>`に作り直し。それでも改善しなかったため、**隠しユーザー名欄**(`autoComplete="username"`・視覚的に完全非表示・読み取り専用)を追加。**値は「AllMarks」**(ユーザー指定。一度「Private」で実装してしまい訂正指示を受けて直した経緯あり — 今後同種の作業では指定された値をそのまま使うこと)。
- **並行して発見・修正した副作用**: `<form>`化で3画面の余白が詰まる視覚退行(テストでは検知不可・レビューで実測発見)→ CSS修正。Playwrightのクリップボード権限をグローバル付与すると別の既存テスト(`mobile-save.spec.ts`の空クリップボード前提テスト)と衝突する問題→ファイル単位に権限スコープを限定。
- **検証体制**: 全工程でopus中心の複数回レビュー(場合によっては2回目の再レビューで1回目の修正の不備を発見)。最終状態: フルスイート**2980/2980**・tsc0・playwright(`private-vault.spec.ts`)16/16・build OK。masterへマージ・push・`allmarks.app`へデプロイ済・**ユーザーが実機で複数回確認済**(既存Private解錠→強制作成→コピー→パスワード忘れて復旧キーで再解錠、まで一通り確認)。

## ★次にやること(優先順位は未定・①②はこのセッション内でユーザーが継続希望)

1. **リアルタイム同期化の可否検討**(まだ結論を出していない・複数セッションにわたり先送り禁止と指定済み)。現状は「タブを閉じる/裏に回す/次に開く」時のみ自動同期。`sync-controller.ts`の`markDirty`(編集20秒後に同期)は仕組み自体はあるが、全保存箇所への配線がゼロ。ユーザーから「コストをかけずに限りなくリアルタイムに近づけられないか」と質問あり → Claudeの回答(未実装・口頭のみ): 常時接続の通知サーバーを持たない設計上、完全リアルタイムはコスト¥0の前提と矛盾するが、既存の`markDirty`を全保存箇所に配線し間隔を詰めることで「数十秒以内」程度まで近づけることは可能。**次回この方針で実装するか、あらためて検討して結論を出す**。
2. **運用セットアップ(ユーザーの手作業)**: (a) claimレコードを1件seed(`wrangler kv key put --binding=K3_KV "claim:<合言葉>" '{"label":"launch","issuedCount":0,"maxIssue":50,"active":true}'`)、(b) ユーザー本人用の無料キーを1本発行して渡す(秘密鍵で直接署名・課金フロー不要)。**ユーザーが「これもきちんとやりましょう」と明言・着手待ち**。合言葉(実際に配る招待コードの文字列)をユーザーに決めてもらう必要あり。
3. **公開前タスクPL-1/PL-2**(下記参照)。**ユーザーに確認済 — これは本人がGoogle Cloud Consoleで行う作業で、Claudeは代行できない**。手順の詳細案内は可能。

## ★公開前タスク(束2で発生・継続)

- **PL-1**: OAuth同意画面のメールを個人Gmail → 専用アドレス(Googleグループ)。memory `project_oauth_support_email_swap`。ユーザー自身の作業。
- **PL-2**: OAuthアプリを「テスト中」→「本番」公開 + Search Consoleで`allmarks.app`ドメイン検証(放置運転自動同期の前に必須。テスト中だとrefresh tokenが7日で失効)。ユーザー自身の作業。

## 恒久ルール(継承)

- 視覚変更は`ui-design.md`「承認後」。`rtk`前置・`--no-verify`禁止・vitest/playwrightは素のnpx(`rtk npx`は既知の不具合)・Framer Motion禁止。
- 音(dotted-notebook)/紙(paper-atelier)＝バイト同一を死守。
- 機微(支援・値付け・戦略)はtrackedに書かない＝`docs/private/`。
- merge/push/deployは必ずユーザー確認後。ただしdeployは「本番で見たい」等の明示的な合図があれば即実行可。docsだけのpushはしない(次の実務pushに同梱)。
- 選択ボックス(AskUserQuestion)はデザイン判断・意思決定・調査/デバッグ中の質問には使わない。普通の会話で聞く。
- 文言(UIコピー)を新規/変更するときは、実装前に実際の英語・日本語の文面そのものを見せて確認を得る。**ユーザーが指定した具体的な値(文字列など)は、実装前にそのまま使うこと — 勝手に別の値に置き換えない**(s217でこの失敗をした教訓)。
- IDB/vaultなど不可逆な本番データに関わる変更は、実行前に必ずユーザーに事実確認する。
- 大規模調査・実装はサブエージェントに委譲(司令塔は診断・設計・指示書・検収)。同一ファイルへの並行実行は避ける(s217で一度、並行タスクの片方が`git stash`を使い相手の作業を一時的に巻き込む事故が発生・データ損失は無かったが要注意)。

## 保留中(同期の後 or 並行)

- **N-78**: 画像無しツイート専用カード(見た目案の提示・承認が必要)。
- **N-64**: カードの＋TAGポップオーバーが再表示後に開けなくなる既存バグ(`CardsLayer.tsx`)。
- **N-63**: `BackupReminder`の表示位置がScrollMeterに被る(モック→承認後)。
- **N-65**: ECDH秘密鍵unwrap時に生バイトが一瞬JS経由(severity LOW・設計変更要)。
- さらなるテーマ/Flat磨き、C2翻訳仕上げ。

## 備考(TODO.mdの肥大化)

`docs/TODO.md`が既に375行程度あり、CLAUDE.mdの目安(200行)を大きく超えている。古いセッション(s196〜s213あたり)のnarrativeを`TODO_COMPLETED.md`へ移す整理作業がまだ手つかず。今回は時間の都合で見送ったが、次に手が空いたタイミングで着手すること。
