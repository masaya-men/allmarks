# 次セッションのゴール — 端末間同期の実装計画づくり

## ★s208 の到達点(端末間同期の設計フェーズ完了・実装コードはゼロ)
- 前セッションから引き継いだ「収益化の仕組み」に着手。superpowers:brainstorming で方針を固め、業界調査を挟んで **端末間同期(複数端末で同じブクマ/タグ/ボードを持つ)** の設計書を作成。
- **設計書 = `docs/private/2026-09-02-device-sync-design.md`(非公開)**。業界調査レポート = `docs/private/raw-research/sync-research.md`。収益まわりの方針更新は `docs/private/IDEAS.md`「s208」節に集約(tracked には書かない)。
- 方式: ユーザー自身の Google Drive にファイルを置く方式(BYOS)。運営はサーバーもデータも持たない・¥0 インフラを維持(理念チェックを設計書 §1 に明記)。Dropbox は後日(phase 2)。
- アルゴリズム: 「まるごとファイル + id で足し算マージ」(取り込みは置き換えではなく合体、追加は絶対に消えない)。本格的な同期エンジンは全てサーバー前提のため不採用、最近似先例 = KeePass。
- 自動同期(放置運転)を最初から。Google のログイン受け渡しだけを中継する極小 Cloudflare Function(`functions/api/gauth/*`、何も保存しない)を1個追加して refresh token を取得。
- Private(金庫)ブクマも同期対象(暗号文 + パスワードで包んだ ECDH 秘密鍵を一緒に運ぶ、別端末で同じパスワードを1回入力して解錠)。設計書 §9 に代償を明記。
- 下ごしらえ(単独でデプロイ可・既存挙動を変えない): `BookmarkRecord` に `updatedAt` 追加(v16→v17 migration、既存は `Date.parse(savedAt)` で埋める)、`TagRecord` にソフト削除印、`CardRecord` に `updatedAt`。
- コード変更ゼロ。詳細は `docs/TODO_COMPLETED.md`「セッション208」節。

## ★次セッション最優先 = 実装計画づくり → 束1着手
1. **superpowers:writing-plans** で設計書を実装計画(束ごとのタスク)に落とす。束の分割は設計書 §14 参照:
   ① 下ごしらえ ② 極小 Function + 認証 ③ アダプタ + マージ(純関数・テスト厚め) ④ エンジン(pull/merge/push) ⑤ 最小 K3 ゲート ⑥ SyncPanel UI ⑦ e2e
2. **ユーザーに Google Cloud 側の初期設定(OAuth クライアント作成)を1回お願いする**必要あり。手順は実装計画に書く(設計書 §4.3 が下敷き)。
3. 実装は **束1(下ごしらえ)から**。単独でテスト・デプロイでき、既存挙動を変えない。
4. 文言(UI コピー)は実装前に実際の EN/JA の文面を見せて承認を取る(束6)。

## 保留中(同期の後 or 並行)
- **N-78**: 画像無しツイート専用カード(見た目案の提示・承認が必要、視覚変更)。
- **N-64**: カードの＋TAGポップオーバーが、フィルタ等で一瞬消えて再表示された後にもう一度開けなくなることがある(`CardsLayer.tsx` の既存バグ)。
- **N-63**: バックアップ提案(`BackupReminder`)の表示位置が ScrollMeter に被るバグ。視覚変更のためモック→承認後に実装。
- **N-65**: ECDH 秘密鍵の unwrap 時、生バイト列が一瞬 JS 側を経由する(severity LOW、設計変更要のため見送り)。
- ResizeHandle の当たり判定が小さいカードで＋TAGボタンを奪うバグ(視覚変更のため承認後)。
- さらなるテーマ/Flat 磨き、C2 翻訳仕上げ(N-74/N-76 で追加した `share.titleToFront/titleToBack` 13言語のネイティブレビューもこれに合流できる)。

## 恒久ルール(継承)
- 視覚変更は `ui-design.md`「承認後」。`rtk` 前置・`--no-verify` 禁止・vitest/playwright は素の npx(`rtk npx` は既知の不具合)・Framer Motion 禁止。
- 音(dotted-notebook)/紙(paper-atelier)＝バイト同一を死守。
- 機微(支援・値付け・戦略)は tracked に書かない＝`docs/private/`。
- merge/push/deploy は必ずユーザー確認後(CLAUDE.md 安全ルール)。ただし deploy は「本番で見たい」等の明示的な合図があれば即実行可。docs だけの push はしない(次の実務 push に同梱)。
- **選択ボックス(AskUserQuestion)はデザイン判断・意思決定・調査/デバッグ中の質問には使わない。普通の会話で聞く。yes/no の確認や「どれから着手するか」のような軽い選択も例外なく対象**。
- **文言(UI コピー)を新規/変更するときは、実装前に実際の英語・日本語(15言語対応時はそれも)の文面そのものを見せて確認を得る。方針・スコープの確認だけで実装に進まない**。
- **IDB/vault など不可逆な本番データに関わる変更は、実行前に必ずユーザーに事実確認する**。
- **サブエージェントへの検証指示には、影響範囲に応じて playwright e2e も含める**。
- **新しい UI 要素(既存の絵文字/アイコン統一など)を追加するときは、関連する全ての表示箇所を機械的に再チェックする**。
- **大規模調査はサブエージェントに委譲**(CLAUDE.md 全体ルール。s208 の業界調査もそうした)。
