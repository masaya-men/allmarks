# 次セッションのゴール (= セッション 105)

## 今のゴール (1 行)

**🎉 session 104 で「保存直後タグ付け 第2段(PiP)+ ON/OFF トグル」完成・本番反映・user 実機確認済(OK 確定)。残りの第2段リワークも全部反映済(SETTINGS=TUNE同一の開き方、PiP タグメニュー=ボードの TagAddPopover を右サブパネルで再利用・カード見えたまま・上から開く・ホバー離脱で閉じる・2/3×3/4 サイズ、衝突回避の pipActive バグ修正、拡張 0.1.20)。次は user が選ぶ: 第3段(ブックマークレット/URL貼り付けに同じ帯)か 公開準備(言語切替・onboarding・LP・拡張ストア素材)。**

## 開始時の動き
1. このファイル + [docs/TODO.md](./TODO.md)「現在の状態 (セッション 104)」を読む
2. **本番は `allmarks.app`**(deploy は `--project-name=allmarks --branch=master`)
3. user に「第3段 / 公開準備、どっち行く?」を確認(第2段は確認済なので再検証は不要)

## 本番 allmarks.app で見てほしい(第2段+リワークの実機確認)
1. **SETTINGS = TUNE と同じ開き方**: ボード右上 SETTINGS に**ホバー**するとTUNEと同じく `max-height` でスッと下に展開(クリックではない)。中の「QUICK-TAG ON SAVE」OFF→保存で帯が出ない/ON で戻る。「OPEN EXTENSION SETTINGS」で拡張設定が開くか(※localhost では開かない作り。必ず本番で)。
2. **衝突解消**: PiP(Pop Out)を開いた状態で保存 → フローティングボタンの帯は出ず、PiP に新カードが入場アニメで入る。
3. **PiP タグ付け(再利用版)**: PiP のアクティブカード左上の「**+ TAG**」(ボードと同じ見た目)→ **ボードと同じタグメニュー(TagAddPopover、新規作成欄つき)**が開く。チップで付与→✓、ボードへ即反映。**+TAG でボードにジャンプしない**ことを確認(前回のバグ)。
4. **タグメニューは PiP の右側の狭いパネルだけ**に出る(全画面オーバーレイは廃止)。**カードは見えたまま**、パネル内が縦1列で内部スクロール(カードは動かない)。パネル外(=透明)クリックで閉じる。
5. **目視で詰める候補**: 右パネルの幅(現 56%/最大240px)・位置(右固定)・「+ TAG」の位置。窮屈/位置が気になれば調整。
6. **拡張は 0.1.20 にリロード**してから確認(②衝突回避はリロード必須)。

## 次の候補(第2段の確認が済んだら)
- **第3段: ブックマークレット/URL貼り付け** — Shadow DOM トースト / `/save` ポップアップに同じ帯を横展開。`/save-iframe` は既に応答に必要情報(tags/quickTagEnabled/pipActive)を持つので相乗り方式そのまま使える。
- **公開準備**: i18n 言語切替の配線(要 brainstorming)/ onboarding / LP 整備 / 拡張ストア公開素材。

## 守ること
- **本番は allmarks.app**。deploy 前 `npx wrangler whoami`、tsc + vitest 通してから。実機/本番で測ってから「動いてる」と報告
- 拡張の content.js / floating-button.js / lib/*.js は `node --check` で構文確認(vitest/tsc 素通りの実例あり)
- 発明しない・本物の部品を流用。横文字を日本語応答に混ぜない。AskUserQuestion ボックス禁止。デザイン変更は提案→承認→実装
- `DB_NAME='booklage-db'`・`booklage:*` メッセージ型 等の不可視符号は**永久に維持**
- i18n: 新 key は 15 言語全部に同期
