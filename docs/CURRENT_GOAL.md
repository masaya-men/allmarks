# 次セッションのゴール (= セッション 144)

## 今の状態（セッション143で完了・allmarks.app 反映済・GitHub push 済）

**(B) 集客/ローンチ計画をブレストで確定・正本化** ＋ **(C) の第1歩「共有画像への allmarks.app 焼き込み＋SAVE IMAGE ボタン」を実装・出荷**。tsc0 / vitest1827 / build OK・default(黒+音波)無傷・commit/push 済。**ユーザー実機確認済**: 共有画像の右下に `allmarks.app`／「N OF M CARDS」撤去／SAVE IMAGE で画像DLできる。

### (B) ローンチ計画 確定（正本＝`docs/private/2026-07-01-launch-plan-design.md`、gitignored・commit しない）
- **形**: ビルド・イン・パブリック → 花火ドロップの二段（完成を待たず0人期間を集客に変える）
- **切り口**: (1)表現ツール主軸 + (3)応援/ソロ開発サブ。**FF14味のオンランプで本人の約600フォロワー（ほぼ全員FF14勢＝ミラプリ=モードボード文化で地続き）を起動**
- **決済**: **FANBOX(日本・円建て) + Patreon(海外) を最初から両対応**
- **解錠 = K3 確定**: 発動回数キャップ（例3台）。¥0の極小ライセンスサーバー（Worker+KV、キーID→回数だけ・ブクマ非接触）。「無限漏れはダメ」を満たす。モデル(A) §4「サーバー無し」→「署名キー＋極小サーバー」へ改訂済
- **ドロップ前に必須な(C)は3つ**: ①プレミアムテーマ2-3本 ②K3ライセンス実装 ③共有画像の焼き込み（=③は今回完了）
- 宿題フラグ: K3のキー配布は Patreon=OAuth自動発行可／**FANBOX=会員確認の公式APIが無い可能性大（実装時確認）**＝手動配布になりがち

### (C) 共有画像の焼き込み 完了（出荷済）
- ShareMirror 下帯を **`allmarks.app`（右下・慣習的な署名位置）** に＝プレビュー/OG画像/保存画像すべてに焼き込み。**SAVE IMAGE ボタン**追加（既出力の1200×628画像をDL→Xに直接ベタ貼り＝リンクカードより大きく目立つ）。内部情報「N OF M CARDS」撤去。キャンバスfallbackも揃え。`shareImageFilename` 純関数+6テスト。default 無傷・share専用。

## 次にやる（セッション144）= (C)の続き or 持ち越し確認
1. **(C) プレミアムテーマ着手** or **K3ライセンスの設計ブレスト**（どちらから行くか要相談。テーマは純制作・並行可／K3は新サーバー部品で単独設計が要る）
2. 持ち越しの実機確認: **N-12 ライトボックス開閉アニメ**（s141）／**ドラッグの軽さ**（s142 窓化後）。まだ重ければ完全 O(N) 化
3. 余力で **N-09 影強度** / **N-10 共有テキストカード紙パリティ** / **N-11 タグ絞り込み最上部の緑**

## 記録済み新アイデア（IDEAS.md・未着手）
- **選択的シェア**（どの100枚を送るか手で選ぶ。今は新しい順100枚固定＝600枚ユーザーに困る。3案記録済）
- **空き盤面を掴んで「ぐりぐり」動かす**パララックス微インタラクション（上澄みpolish）

## 守ること（毎回）
- default 盤面 byte-identical。deploy 前 `rtk tsc && rtk vitest run && rtk pnpm build`。deploy `--project-name=allmarks --branch=master`
- **収益化・ローンチ戦略は機微＝`docs/private/`（gitignored）のみ。tracked に書かない・commit しない**（CLAUDE.md 厳守）
- 台紙系・装飾・共有画像は実描画(playwright/実機)を見てからデプロイ。board のドラッグ/カードクリックは playwright 不可
- 既知フレーキー: `tests/lib/channel.test.ts`（再実行で緑）。応答は日本語、簡潔に
