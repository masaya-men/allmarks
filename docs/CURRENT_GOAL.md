# 次セッションのゴール — Private Phase 2 完全完了、次はユーザー実機確認 or 他タスク

## ★s204 の到達点(Private Phase 2、①〜②まで完全完了)
- **クイック保存面(PopOut/拡張機能/ブックマークレット)対応、拡張機能UI配線、多言語化まで全部完了。`master`へmerge済・GitHubへpush済・本番デプロイ予定。**
  - 設計を「対称鍵暗号」から「公開鍵暗号(ECDH)」へ全面ピボット。タグ付け(暗号化)はパスワード不要でどこからでも可能、閲覧・削除(復号)のみパスワードが必要、という理想形を実現。
  - security-review(4件の実在する脆弱性を修正)・最終全ブランチレビュー(opus、さらに2件修正)を経て安全性を確認済み。
  - 本番の古いPhase1 Private設定をユーザー確認の上で安全に削除(DevTools手作業、EXPORTバックアップ済)。
  - **拡張機能のfloating-buttonにもPrivateチップを配線**(N-66=再送ポンプの落とし穴も同時に修正)。PopOut/ブックマークレット/拡張機能の3経路とも動作する状態。
  - **PopOut/ブックマークレットのPrivateチップに成功/失敗フィードバックを追加**(N-67修正、押しても反応が無かった問題)。
  - **Private関連の文言を15言語に対応**(セットアップ/解錠/共有確認ダイアログ、通知文)。英語・日本語は精査済み、他13言語はAI下訳(公開前にnative reviewが必要、既存の慣例通り)。作業中に見つけた「共有確認の件数表示が単数/複数を間違えて壊れていた」バグもe2eテストで検知して直した。
  - post-plan gate: tsc0 / vitest 300ファイル2509テスト全緑(既知の無関係flaky=`channel.test.ts`のBroadcastChannelタイミング起因は再確認済み) / playwright 102 pass・5 skip / `pnpm build`成功。

## ★次回ユーザーに確認してほしいこと(最優先)
1. `allmarks.app`をハードリロード
2. FilterPillの「Private」行が「未設定(SETUP)」表示になっているか、新しいパスワードで作り直せるか
3. PopOut・拡張機能・ブックマークレットの3つ全部からPrivateタグ付けができるか(拡張機能は今回新規配線)
4. 言語を切り替えて、Private関連の画面がちゃんと翻訳されているか(日本語以外は下訳なので、明らかにおかしい箇所があれば教えてほしい)

## 新規(s204ユーザー指摘、いったん後回し・理想仕様は記録済み)
- **Privateパスワードの再設定・変更フロー**: 忘れた/作り直したい場合の正規の救済手段がアプリ内に無い。理想仕様(変更/作り直し/復元コード方式の3段構え)を`docs/private/IDEAS.md`「⑤ Privateパスワードの再設定・変更フロー」に記録済み。着手はユーザー判断で後日、必ずbrainstormingから。

## 保留中(いつでも合流可)
- **N-64**: カードの＋TAGポップオーバーが、フィルタ等で一瞬消えて再表示された後にもう一度開けなくなることがある(`CardsLayer.tsx`の既存バグ、Private機能とは無関係)。
- **N-63**: バックアップ提案(`BackupReminder`)の表示位置がScrollMeterに被るバグ。視覚変更のためモック→承認後に実装。
- **N-65**: ECDH秘密鍵のunwrap時、生バイト列が一瞬JS側を経由する(severity LOW、修正は設計変更が要るため見送り)。
- ResizeHandleの当たり判定が小さいカードで＋TAGボタンを奪うバグ(s203由来、視覚変更のため承認後)。
- TODO.mdが900行超(目安200行を大幅超過)→ 古いセッションnarrativeをTODO_COMPLETED.mdへ移動する軽い掃除タスク(非ブロッキング)。
- さらなるテーマ/Flat磨き、支援まわり(非公開`docs/private/IDEAS.md`)、C2翻訳仕上げ(Private文言の13言語ネイティブレビューもこれに合流できる)。

## 恒久ルール(継承)
- 視覚変更は`ui-design.md`「承認後」。`rtk`前置・`--no-verify`禁止・vitest/playwrightは素の npx(`rtk npx`は既知の不具合)・Framer Motion禁止。
- 音(dotted-notebook)/紙(paper-atelier)＝バイト同一を死守。
- 機微(支援・値付け・戦略)はtrackedに書かない＝`docs/private/`。
- merge/push/deployは必ずユーザー確認後(CLAUDE.md安全ルール)。ただしdeployは「本番で見たい」等の明示的な合図があれば即実行可。
- **選択ボックス(AskUserQuestion)はデザイン判断・意思決定・調査/デバッグ中の質問には使わない。普通の会話で聞く**。
- **IDB/vaultなど不可逆な本番データに関わる変更は、実行前に必ずユーザーに事実確認する**(s204でPhase1旧vault削除の際に実践)。
- **サブエージェントへの検証指示には、影響範囲に応じてplaywright e2eも含める**(s204で「vitest/tscだけ指示→e2eで初めて発覚したバグ」を経験、教訓化)。
