# 次セッション(s204)のゴール — Private Phase 2 の続き(②クイック保存面 or 残タスク、着手前にbrainstorming必須)

## ★s203 の到達点
- **Private Phase 2 サブプロジェクト1(①発見導線＋③まとめてPrivate化)、完全に完了。`master`へmerge済(`--no-ff`)・本番デプロイ済・ユーザー実機確認済。GitHubへのpushはこのセッション終了時に実施。**
  - 6タスクのplanをsubagent-driven-developmentで実装。各タスクレビュー＋Task4/6でそれぞれ1回fix round。最終全ブランチレビュー(opus)→Important 2件(パスワード忘れ救済のヒント文が新3経路で出ない／キャンセル時に保留アクションが残る)を修正、UI見た目に関わる1件(カード自体のPrivateピル消失)はユーザー確認待ちで保留。
  - **本番デプロイ後、ユーザーの実機フィードバックから2件の追加バグを発見・修正・再デプロイ**:
    1. カードのホバー時ピル表示が一部状況でPrivateタグだけ出ない問題 → 直したところ、副作用で「右クリックメニュー(削除/リネーム)が今回初めて到達可能になっていた」ことをレビューが検出 → 追加修正で抑制。
    2. ユーザー報告「暗号化に3時間かかったような挙動」→ 実際は**Reactの古いクロージャがreload()を握ったままレースする表示バグ**(暗号化自体は一瞬で正常)。Playwrightで実際に再現→根本原因を特定→回帰テストつきで修正。憶測で終わらせず実機再現までやった。
  - **ユーザー要望でPrivateの並び順を変更**: 3箇所(FilterPill・カード＋ボタン・MANAGE TAGS)とも、スクロールする本来のタグ一覧の**中**に、実タグの一番後ろ(並び替えモード対象外)として組み込み。タグが増えるほど自然に下へ隠れる設計に。モバイルのMANAGE TAGSは元から正しい形だった。

## ★保留中(次回以降、着手前にユーザー確認/承認が要る)
- **ResizeHandleの当たり判定バグ**: カード左上のリサイズハンドルの装飾用オーバーレイが、小さいカードだと＋TAGボタンの当たり判定を奪うことがある(s203最終レビューでPlaywrightの実診断テストにより確認済み・既存の別バグ・Private機能とは無関係)。修正には当たり判定/位置の調整が要り見た目に関わるため、着手前にモック→承認。

## ★次セッション最優先＝ Private Phase 2 の続き、着手前に必ずsuperpowers:brainstormingから
s203時点での合意済み順番: **②クイック保存面対応(PopOut/拡張機能/ブックマークレット、案B=鍵の安全な受け渡し)が次点**。技術的に一番重い部分(別ウィンドウへの鍵の安全な受け渡し)。その後に④(存在を隠すオプション)。詳細は`docs/private/IDEAS.md`「s202 Private Phase 2構想まとめ」節。

「さらにプライベートについて完成度をあげて」というユーザーの指示なので、②から始めるのが自然だが、下記の軽微な積み残しを一緒に拾ってもよい(ユーザーと相談):
- `PrivateSetupDialog`の説明文がaria-describedbyに繋がっていない(スクリーンリーダー未対応)
- `TriagePage.tsx`に`BoardRoot.tsx`と同じ除外パターンの別名ローカル変数がある(命名不一致、実害なし)
- `BoardRoot.tsx`の`assignTagToCards`ガード付近のコメントが古い(「UIは存在しない」→今は存在する、ガード自体は正しいまま)
- ロック中→解錠→自動再開、のe2eカバレッジが薄い(未設定→解錠は厚いが、ロック中からの再開は手薄)
- ロック中に新規タグ入力欄へ「private」と入力すると、本物のPrivateタグとは別の平文タグが作れてしまう(Phase 1由来の既存の穴、今回のスコープでは未対応)

## 次点(Private Phase 2と並行 or 別途、いつでも着手可)
- **N-63**: バックアップ提案(`BackupReminder`)の表示位置がScrollMeterに被るバグ。中央+最前面に変更希望。視覚変更のためモック→承認後に実装(`docs/TODO.md`§未対応バグ)。
- **Lightbox複数画像ツイートの理想形**: 「開く瞬間だけ盤面と揃えて切り取り／開いた後は手動切り替えで全体表示」への改善。詳細`docs/private/IDEAS.md`「s202 複数画像ツイートのLightbox内ブラウズを...」節。

## 保留中(いつでも合流可)
- TODO.mdが900行超(目安200行を大幅超過)→ 古いセッションnarrativeをTODO_COMPLETED.mdへ移動する軽い掃除タスク(非ブロッキング)。
- さらなるテーマ/Flat磨き、支援まわり(非公開`docs/private/IDEAS.md`)、拡張の一括保存、C2翻訳仕上げ。

## 恒久ルール(継承)
- 視覚変更は`ui-design.md`「承認後」。`rtk`前置・`--no-verify`禁止・vitest/playwrightは素の npx(`rtk npx`は既知の不具合)・Framer Motion禁止。
- 音(dotted-notebook)/紙(paper-atelier)＝バイト同一を死守。
- 機微(支援・値付け・戦略)はtrackedに書かない＝`docs/private/`。
- merge/push/deployは必ずユーザー確認後(CLAUDE.md安全ルール)。ただしdeployは「本番で見たい」等の明示的な合図があれば即実行可。
- **選択ボックス(AskUserQuestion)はデザイン判断・意思決定・調査/デバッグ中の質問には使わない。普通の会話で聞く**(s203でユーザーから明示指摘、`feedback_no_question_box_for_debugging`参照)。
