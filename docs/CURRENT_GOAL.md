# 次セッションのゴール — 既存のバックログ（＝作った計画）をリリースに向けて実行する

## 進め方（最速プロトコル）

- **1 機能ずつ、フレッシュなセッションで**進める。設計は最小往復で最速、実装→検証→デプロイまで自走。
- 見た目変更は claim 前に Playwright で実測（ユーザーが「確認は自分でやる」と言ったら省略可）。応答は短く。戻せる修正は逐一承認を取らず自走。デプロイは `--branch=master`。
- **機微（収益化・価格・ローンチ戦略・競合）は必ず `docs/private/IDEAS.md`**。tracked ファイルには書かない。

## ★次セッションのゴール＝ 下のバックログを上から実行（新しく計画は立てない）

計画はもうある（このバックログ＋各 spec/plan）。**次はそれを実行してリリースに近づける**。棚卸し・計画づくりのセッションではない。

**最初にやること（Claude の動き）**:
1. このファイル＋ `docs/TODO.md` を読み、下の候補から**着手する1つ**を決める（ユーザーに「今回はこれをやります／どれからやりますか」を1行確認）。
2. 拡張・共有・収益化に触るなら関連の `docs/private/IDEAS.md`（**#5 収益化 / ローンチ / K3 / スマホ Web Share ヘルパー**）を先に読む。
3. 決めた1つを **実装→検証→デプロイまで自走**（既存の spec/plan があればそれに従う）。

**公開前ゲート（実行対象・リリース必須）**:
- **13言語の翻訳レビュー（特に Terms の法的条項）**＝s161 でたたき台のまま。ワイドローンチ前にネイティブ＋法務レビュー必須。
- `EXTENSION_STORE_URL` 投入＝Chrome 審査通過待ち（外部要因・通ったら1行）。
- 技術的な公開前片付けは実質ゼロ（s129 で実態確認済・TODO.md「ドメイン」節）。

## 直近の完了（s176）＝ SHARE 自動画像化 出荷＋「本物のボード枠込み」化（本番OK）

- **SHARE = 「①選ぶ ②並べる ③作る」の1ボタン**。手動スクショ撤廃。本物のアレンジ画面を dom-to-image で自動撮影（同一オリジン画像 proxy `functions/api/img.ts` でクロスオリジン汚染回避）→ 1200×630 → 既存 R2／`/og/<id>.jpg` → LINK READY。
- **追補**: 共有画像は本物のボード枠込み（左上ロゴ入り／右上メニューは撮影時に隠す）。カードはボードの端まで移動・拡大でき、枠でクリップ（浮かない）。
- 本番 edge 実測＋ユーザー実機確認まで完了。詳細 `docs/TODO_COMPLETED.md` s176（＋追補）／`docs/private/IDEAS.md`「SHARE 自動画像化」§。
- 残る任意手動: X に貼って大画像カードが出るか目視（投稿時でOK）。

## 実行する候補（この中から1つ選んで着手・順不同）

- **スマホ本格対応**（以前「最優先」・s161）＝規模大。「まず盤面閲覧＋保存だけ」の第1弾に切って着手。#8 の Web Share ワンタップ共有（ヘルパー実装済＝配線するだけ）もここで。
- **タグ強化**（s60 で最優先だった整理の背骨）＝速めに効く・比較的軽い。
- **TUNE テーマ追従（案C・全メニュー一括）** をやるか、s163 中立化を維持するか（着手前に方針だけ即決）。
- **#5 収益化 / ローンチ導線**：機微につき `docs/private/IDEAS.md`（#5 / K3 / ローンチ）参照。
