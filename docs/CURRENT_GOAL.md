# 次セッションのゴール (= セッション 40)

## ゴール

**user 指定なし** — backlog から最優先 1 件を選んで着手。 候補は順番に:

1. **multi-playback vision の board card autoplay 着手** (= 差別化機能 core、 memory `project_allmarks_vision_multiplayback.md`)
2. **テキストカード Lightbox 構造再設計** (= session 36-38 で局所 fix を重ねた領域、 まとめて作り直す機会)
3. **B-#3 重複 URL でサムネ等が出ない問題** (= 古めの未解決、 真因未調査)
4. **B-#13 TopHeader 上部 chrome brushup** (= session 39 で bottom 側 / slider 周りは決着済、 残は上の filter pill / share 等のレイアウト)

## 開始時の動き

1. user と挨拶 + session 39 close-out 確認 (= `booklage.pages.dev` をハードリロード)
   - meter: 場所固定 + close で「家に帰る」 ぬるっと glide、 close click 判定良し
   - slider: NNNN.NN 表示、 click ジャンプ、 Shift で高速、 hover で 拡張機能風 glass tooltip がマウス追従
2. 候補 1〜4 のどれをやるか user に聞く
3. 決まったタスクの spec / plan / memory を読む
4. 提案 → 合意 → 実装 → tsc + vitest → prod deploy → user 確認

## 月末リマインダー (= 2026-05-31 約 2 週間後)

`allmarks.app` ドメイン取得確認。

## 引き継ぎ resources

- [docs/TODO.md](docs/TODO.md) — active backlog 全体
- [docs/TODO_COMPLETED.md](docs/TODO_COMPLETED.md) セッション 39 セクション (phase 1-12) — meter refactor + slider 改修 narrative
- memory `feedback_layman_simple_path.md` — user の「素人考えですが」 提案を 1 段重く受け取る教訓

## 余裕があれば backlog

上記 1〜4 のうち、 着手しなかったもの。
