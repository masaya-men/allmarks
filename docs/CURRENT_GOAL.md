# 次セッションのゴール — Fable で「s186 新規4件」の完璧な計画書を書き切る（実装はしない）

## ★セッションの始め方（ユーザー確定 s186）

1. **Fable を使う。実装・並行作業は一切しない**。計画書がすべて書き終わるまで計画に専念する。
2. 粒度の基準は s185/s186 と同じ＝**最低ランクの安価なモデル（Haiku 等）が迷わず正確に、ミスなく写経で実装できる**こと（実ファイルパス・実コード・実テスト・実コマンド・期待出力まで埋める。判断を実装者に残さない）。
3. 先に読む: この文書 → [ロードマップ](superpowers/plans/2026-07-11-s186-remaining-work-roadmap.md) → `docs/private/IDEAS.md` の **s186 節**（3アイデアの実現性調査済みメモ）→ 各対象コードは並行 Explore エージェントで行番号つき採取（s186 と同じやり方）。

## 計画書を書く対象（4件・この順で）

1. **(N-58 段階2) ピンチズーム＋パン** — ゴール確定済み: スマホでも 100 枚上限でステージをピンチ拡縮・パンしながら編集。方式確定済み: 編集中だけ wrapper に CSS transform、撮影直前にリセット（撮影系は1行も触らない）。ドラッグ量を zoom で割る配線・2本指=ステージ/1本指=カードの判別が本体。[n58 計画](superpowers/plans/2026-07-11-n58-mobile-collage-editing.md)の設計判断節に前提を書き込み済み。
2. **(BULK-IMPORT 第1弾) 拡張の一括取り込み: X ブクマ＋YouTube 高評価/後で見る** — 方針確定済み「対応サイトは基本全部、拡張で一括取り込み」。**注意: s49 Pinterest の教訓＝実 DOM を採ってから書く**。計画書には「ユーザーのログイン済みブラウザで DOM を採取する手順タスク」を必ず含める（採取前に selector を確定させない）。キュー＋進捗＋中断再開＋OGP レート制御。再審査は N-28/N-29 と束ねる前提。
3. **(CUTOUT) 切り抜きコラージュ（手動なげなわ＝MVP）** — clipart.studio 型。AI 背景除去は後段（計画対象外・言及のみ）。保存の形（素材の置き場）・コラージュ統合の設計判断があるので、**計画前に brainstorm 相当の設計確認をユーザーと散文で行ってから** plan 化。
4. **(SHADER-THEME) WebGL 擬似3Dテーマ** — 参照は IDEAS.md s186 節（Forza 風の深み・超軽量）。見た目の方向はユーザー承認が要る（ui-design ルール）＝**モック/参考の提示→承認→plan** の順。MOTION OFF 完全停止・4K fill-rate 予算・既定テーマ不変が制約。

各計画書に推奨モデル（Haiku/Sonnet/Opus）をタスク単位で明記すること（s186 の5本と同じ流儀）。

## 設計判断はユーザーと散文で（質問ボックス連打をしない）

3 と 4 は見た目・データ形の判断が要る。**一度に1つずつ、平易な日本語の散文**で相談してから計画に固める（memory `feedback_no_question_box_for_decisions`）。1 と 2 は判断済みなので相談なしで書き切れる。

## 計画が全部終わったら（実行フェーズの順序・確定済み）

**N-56 実行**（★★ブロッカー・[計画済み](superpowers/plans/2026-07-11-n56-mobile-share-image-fix.md)・Task 0 回答記入済み: iPhone Safari・4枚でもプレビュー不生成）→ N-58 段階1 → N-58 段階2 → N-57+59 → N-54 → N-53 → 束C〜E（公開）。新規4件の実行時期は計画完成後にロードマップへ差し込んでユーザーと確定。

## 絶対に守ること（恒久ルール・継承)

- 撮影は実機でしか検証できない／ボードを変えたら受け取り画面も確認／`fit:'cover'` 固定／`CardsLayer` に `isMobile` 渡さない／デスクトップ 1px 不変／`rtk` 前置・`--no-verify` 禁止／機微は `docs/private/` へ。
