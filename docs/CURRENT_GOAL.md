# 次セッションのゴール — 端末間同期、束6③完了。マージ判断が次。★公開前に必修正2件あり(下記)

## ★s215 の到達点(束6③=vault食い違いUI・isPrivateVaultタグ重複安全網・EMPTY TRASH文言、全10タスク+最終ブランチレビュー+修正完了)

- **束6③実装・全タスクレビュー・最終ブランチレビュー(opus)・修正1波・スコープ再レビュー・全て完了**(`feat/vault-conflict-resolution`ブランチ)。フルスイート**2922/2922**・tsc0・playwright(`private-vault.spec.ts`)11/11・eslint新規混入なし。**まだmasterマージ前**(このブランチのまま次セッションへ引き継ぎ、または今セッション内でマージ/PR判断に進む)。詳細=TODO.md s215ログ参照。
- **束6②(SyncPanel UI本体)はこのセッションより前にmasterマージ済・push済・本番デプロイ済**(`allmarks.app`)。CURRENT_GOAL.mdの旧版に残っていた「まだmasterマージ前」の記述は古い誤り(このメモで訂正)。

## ★★公開前(claimレコードseed=誰かが同期を使える状態にする前)に必ず直すこと2件(最終レビューで発見・今回は未修正)

現状は**同期そのものがK3ライセンスで完全にゲートされており未発行=実害ゼロ**なので今回は見送ったが、**運用セットアップ(下の②)に着手する前に必ず解決すること**:

1. **vault(金庫)食い違い検知が非対称**: どちらの端末が先に`vault.json`をDriveに公開するかという(`pickDeterministic`の勝敗とは無関係な)偶然のタイミングによって、先に公開した側は自分の`vault.json`を読み返すだけになり食い違いを一生検知しない。もし「先に公開した側」=「pickDeterministicの勝ち側(まとめ先)」が偶然一致した場合、まとめ先の端末には`vault-conflict-notice`/`vault-conflict-resolved`画面が一生出ず、「まとめた後に新しいパスワードを1つ決める」という設計の核心的な約束が半分程度のケースで果たされない。**設計からの見直しが必要**(調査済みの方向性: 検知の起点を対称な信号=`allPrivateTagIds.size > 1`に変える。ただしこの場合`otherRecord`の再構成方法など未設計)。詳細=このメモの真実の記録場所である`.superpowers/sdd/2026-09-16-device-sync-bundle-6c-vault-conflict/progress.md`のTask10直後の「Final whole-branch review」節、I-3。
2. **まとめ先のパスワードが本当に解錠可能か、確認せずに公開・上書きしてしまう**: まとめ先は公開鍵だけで(解錠せずに)`vault.json`を自動公開する。まとめ先のパスワードが本当に失われていた場合、両端末のPrivateが永久に読めなくなる(復元手段が現状無い)。加えて、Googleアカウント自体が乗っ取られた場合、攻撃者が`pickDeterministic`の文字列比較を意図的に勝たせてPrivate全体を攻撃者の鍵に付け替えさせる余地がある(前提としてGoogleアカウント乗っ取りが必要=既存のBYOS設計が元々受け入れているリスクの範囲内だが、影響範囲が広がる)。**これは製品判断(パスワード復旧コードの仕組みを追加するかどうか)であり、コード修正だけでは閉じない**。docs/private/IDEAS.mdへの記録、または方針確定が必要。

## ★次にやること(この3つのうち、ユーザーが選ぶ — どれを優先するかは決めていない。★ただし②に着手する前に上記2件の解決が必須)

束1〜6③(下ごしらえ〜vault食い違いUI)が揃うと、端末間同期の機能自体は一区切り。残るのは以下3つの独立した項目:

1. **リアルタイム同期化の可否検討**(まだ結論を出していない・先送り禁止と指定済み)。現状は「タブを閉じる/裏に回す/次に開く」時のみ自動同期。`sync-controller.ts`の`markDirty`(編集20秒後に同期)は仕組み自体はあるが、全保存箇所への配線がゼロ。配線するかどうか・コスト・採用判断をこの場で検討して結論を書く。
2. **運用セットアップ(ユーザーの手作業)**: (a) claimレコードを1件seed(`wrangler kv key put --binding=K3_KV "claim:<合言葉>" '{"label":"launch","issuedCount":0,"maxIssue":50,"active":true}'`)、(b) ユーザー本人用の無料キーを1本発行して渡す(秘密鍵で直接署名・課金フロー不要)。**★上記の必修正2件を先に解決してから**(claimを配ってしまうと誰かがvault食い違いに実際に遭遇しうる状態になる)。
3. **公開前タスクPL-1/PL-2**(下記参照)。

## ★検討事項(ユーザー指定・忘れないこと)

- **リアルタイム反映(タブを開いたまま・編集直後に他端末へ即反映)が可能かどうか、必ず検討・記録すること**(2026-09-16 ユーザー指定)。上の「次にやること」①と同じ項目 — ここでも重複して記録し、先送りのまま忘れられることを防ぐ。

## ★公開前タスク(束2で発生・継続)

- **PL-1**: OAuth同意画面のメールを個人Gmail → 専用アドレス(Googleグループ)。memory `project_oauth_support_email_swap`。
- **PL-2**: OAuthアプリを「テスト中」→「本番」公開 + Search Consoleで`allmarks.app`ドメイン検証(放置運転自動同期の前に必須。テスト中だとrefresh tokenが7日で失効)。

## 恒久ルール(継承)

- 視覚変更は`ui-design.md`「承認後」。`rtk`前置・`--no-verify`禁止・vitest/playwrightは素のnpx(`rtk npx`は既知の不具合)・Framer Motion禁止。
- 音(dotted-notebook)/紙(paper-atelier)＝バイト同一を死守。
- 機微(支援・値付け・戦略)はtrackedに書かない＝`docs/private/`。
- merge/push/deployは必ずユーザー確認後。ただしdeployは「本番で見たい」等の明示的な合図があれば即実行可。docsだけのpushはしない(次の実務pushに同梱)。
- 選択ボックス(AskUserQuestion)はデザイン判断・意思決定・調査/デバッグ中の質問には使わない。普通の会話で聞く。
- 文言(UIコピー)を新規/変更するときは、実装前に実際の英語・日本語の文面そのものを見せて確認を得る。
- IDB/vaultなど不可逆な本番データに関わる変更は、実行前に必ずユーザーに事実確認する。
- 大規模調査・実装はサブエージェントに委譲(司令塔は診断・設計・指示書・検収)。

## 保留中(同期の後 or 並行)

- **N-78**: 画像無しツイート専用カード(見た目案の提示・承認が必要)。
- **N-64**: カードの＋TAGポップオーバーが再表示後に開けなくなる既存バグ(`CardsLayer.tsx`)。
- **N-63**: `BackupReminder`の表示位置がScrollMeterに被る(モック→承認後)。
- **N-65**: ECDH秘密鍵unwrap時に生バイトが一瞬JS経由(severity LOW・設計変更要)。
- さらなるテーマ/Flat磨き、C2翻訳仕上げ。
