# 次セッションのゴール — 高速バックログ消化（1機能 = 1 フレッシュセッション）

## 進め方（最速プロトコル）

- **1 機能ずつ、フレッシュなセッションで**進める（文脈が軽い＝Claude が速く・安く・正確に走れる）。
- 各機能の最初に、ユーザーはキックオフ 1 行を貼るだけ：
  > 「docs/CURRENT_GOAL.md を読んで、バックログの **#N** を進めて。設計は最小往復で最速、実装 → 検証 → デプロイまで自走して。」
- Claude は：**設計（brainstorming・決定は最小）→ 実装 → tsc/vitest/build/deploy → commit → 「次のキックオフ文」を提示**。
- ルール：見た目変更は claim 前に Playwright で実測。応答は短く。戻せる修正は逐一承認を取らず自走。デプロイは `--branch=master`。機微は `docs/private/`。

## 直近の完了（s174 この会話）＝ #8 共有画像の一時保管「画像＋リンクで SNS 投稿・X でも確実に画像表示」**出荷・本番反映済**

- ユーザー発案の流れ「自分でスクショ→貼付/ドロップ→共有リンク作成→X投稿/コピー」を実装。`/s/<id>` のプレビューが**本物のコラージュ**になる（今まで汎用カードだった）。
- **X で確実に画像を出す土台**（姉妹プロジェクト LoPo の実証レシピ移植）：OG 画像を `/api/` の外の静的風 URL **`/og/<id>.jpg`** から **リダイレクトせず 200 直接**配信＋作成直後にキャッシュ温め。`twitter:card=summary_large_image` は既存。
- 検証：tsc0 / vitest 全緑 / クリーンビルド OK。**本番実測で全経路 OK**（`.jpg` ルーティング・`/og` が実 JPEG 200・`/s` メタ・Playwright で貼付→CREATE→LINK READY）。
- **★ユーザーの唯一の残タスク**：**実際に X（Twitter）に共有リンクを1回貼って、大きい画像カードが出るか目視確認**（クローラー相手で自動検証不可）。出れば #8 完全クローズ。出なければ報告 → 追加調整（例：Discord 等では確実に出る）。
- **後日のおまけ（スマホ本格対応セッションで）**：スマホのワンタップ Web Share（画像添付投稿）。ヘルパーは実装・テスト済で UI 配線だけ。

## バックログ（順不同・どれからでも）

- **スマホ本格対応**（あなたが以前「最優先」と言った件・s161）＝規模大。やるなら「まず盤面閲覧＋保存だけ」の第1弾に切る。#8 の Web Share ワンタップ共有もここで配線。
- **タグ強化**（s60 で最優先だった整理の背骨）＝速めに効く。
- **TUNE テーマ追従（案C・全メニュー一括）** をやるか、s163 中立化を維持するか **まず決める**（TUNE だけ追従は他メニューとちぐはぐ）。
- **#5 収益化**：機微につき `docs/private/IDEAS.md` 参照。
- **#6 ライトボックス自動再生＝見送り済**（要否低。再挑戦は静止スライドショーの軽版から）。
- （任意・小）**角丸 OFF を SHARE のコラージュ/共有画像にも反映**：CollageCanvas は `var(--card-radius,20px)` フォールバックで盤面トグルに追従しない。必要ならユーザー判断で追加。
