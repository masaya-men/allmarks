# 次セッションのゴール (= セッション 114)

## 今のゴール (1 行)

**✅ セッション 113 で ①ライセンス AGPL-3.0 整備 ②拡張ストア提出ドキュメント作成 ③貼り付け保存(Ctrl+V)を設計→実装→本番反映 まで完了。次は 日本語コピー一括修正(決定済み・15言語)+ 貼り付け保存の実機確認 + 拡張ストア素材。**

## 開始時の動き
1. このファイル + [docs/TODO.md](./TODO.md)「現在の状態 (セッション 113)」を読む
2. `git branch --show-current` で master 確認(feat/paste-to-save はマージ済・削除済)
3. ユーザーに方向確認 → 下記から着手

## 最優先: 貼り付け保存の実機確認 (ユーザーにやってもらう)
本番 `allmarks.app/board` をハードリロードして:
- 普通サイトの URL をコピー → ボードにフォーカスして Ctrl+V → 読み込み中(音波)→ カードが新着ハイライトで着地、サムネ表示
- YouTube / ツイートの URL を貼る → 取得待ちなしで生きたカード
- 同じ URL を再度貼る → `Already saved`(カード増えない)
- タグ入力欄に focus した状態で Ctrl+V → 普通の文字貼り付け(取り込まれない)
- bot 拒否サイトの URL → フォールバックカードで保存される

## 次: 日本語コピー一括修正(決定事項は確定済み・15言語へ反映)
セッション 113 でユーザーと確定した修正方針は [TODO.md](./TODO.md) の「コピー修正バックログ(確定)」参照。貼り付け保存が実装できたので、保存セクションを正直に書ける(3経路: 貼り付け Ctrl+V / ブックマークレット / 拡張)。確定分を ja で直し、同義を14言語へ。

## 次: 拡張ストア素材の判断 2 点(セッション 113 で保留)
1. 掲載言語: 英語のみで提出 or 英語+日本語
2. スクリーンショット/プロモタイル: Playwright でボード撮影 + on-brand タイル生成を Claude がやるか、ユーザー用意か
提出原稿一式は [docs/extension-store-submission.md](./extension-store-submission.md) に完成済み。

## 守ること
- 本番 = `allmarks.app`。deploy 前 `npx wrangler whoami`、`rtk tsc && rtk vitest run && rtk pnpm build`。`--branch=master --commit-message`(ASCII)必須。`tsc <file>` 直叩き禁止 → `rtk tsc`。
- アプリ本体(`/board` `/s/*` `/save`)に言語接頭辞を付けない。`DB_NAME` 等の内部符号は不変。
- 個人情報(本名/個人メアド/個人 X 垢)を tracked ファイルに書かない。デザイン変更は提案→承認。応答は日本語。
