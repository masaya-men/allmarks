# 次セッションのゴール (= セッション 145)

## 今の状態（セッション144で完了・allmarks.app 反映済・GitHub push 済）

**Paper テーマの動画カード＆装飾を実機対話でブラッシュアップ完了**。tsc0 / vitest 緑（`channel.test.ts` 既知フレーキー→再実行緑）/ build OK・**default(黒+音波)無傷**（全変更 paper-scoped）。8ファイル・1コミット。

### セッション144でやったこと（全てユーザー実機確認済）
- **① 動画カード(YouTube/TikTok)に Paper 台紙**：VideoThumbCard が `paper` 未対応で台紙無しだったのを、ImageCard の paper 構造流用で修正（ImageCard 無編集＝byte-identical）。
- **② YouTube ショートが横長カードで保存される不具合**：サムネ実寸で 9:16 を打ち消していた→shorts は上書きスキップ＋誤保存修復、`cover` で黒帯クロップ。
- **③ Paper 装飾を全面リデザイン→最終「超シンプル」**：各カード必ず「上端中央テープ1枚 or 画鋲1本」だけ（無し廃止）。「板に貼り付ける」＝テープが縁をまたぎ板↔紙を橋渡し（本物の破れシート素材を playwright 計測して確定）。透明テープに落ち影＋光沢で視認性UP。不良マステ washi-tape-8 を bbox 計測で除外。テープ柄はカード単位で統一。
- **持ち越し消化**：N-12 開閉アニメ／ドラッグ軽さ = ユーザーOK。

## 次にやる（セッション145）= (C)の続き（前回からの持ち越し・「生計を立てる」本丸）
1. **どちらから行くか要相談**：
   - **(C) プレミアムテーマ着手**（純制作・並行可） or
   - **K3ライセンスの設計ブレスト**（新サーバー部品で単独設計が要る。宿題フラグ: FANBOX は会員確認の公式APIが無い可能性大＝手動配布になりがち。正本 `docs/private/2026-07-01-launch-plan-design.md`）
   - ※前回セッション143の推奨は「②K3 設計ブレストを先に」（唯一の未知＝サーバー＋FANBOX API を先に潰す）。今回セッションは装飾ブラッシュアップに全部使ったので未着手。
2. 余力で **N-09 影強度** / **N-10 共有テキストカード紙パリティ（ShareMirror）** / **N-11 タグ絞り込み最上部の緑**

## 記録済み新アイデア（IDEAS.md・未着手）
- **選択的シェア**（どの100枚を送るか手で選ぶ。今は新しい順100枚固定＝600枚ユーザーに困る。3案記録済）
- **空き盤面を掴んで「ぐりぐり」動かす**パララックス微インタラクション（上澄みpolish）

## 守ること（毎回）
- default 盤面 byte-identical。deploy 前 `rtk tsc && rtk vitest run && rtk pnpm build`。deploy `--project-name=allmarks --branch=master`
- **収益化・ローンチ戦略は機微＝`docs/private/`（gitignored）のみ。tracked に書かない・commit しない**（CLAUDE.md 厳守）
- 台紙系・装飾・共有画像は実描画(playwright/実機)を見てからデプロイ。**装飾は実素材PNGで縁位置を計測してから配置**（推測で3回外した教訓）。board のドラッグ/カードクリックは playwright 不可
- 既知フレーキー: `tests/lib/channel.test.ts`（再実行で緑）。応答は日本語、簡潔に
