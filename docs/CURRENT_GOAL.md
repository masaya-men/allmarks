# 次セッションのゴール — N-74/76から1つ選んで着手

## ★s206 の到達点(N-69・N-75完了)
- **N-69**(タグ絞り込みに「タグ無し」フィルタ「NO TAGS」): 実は仕組みは既に完全配線済みで、絞り込みメニューにボタンが無いだけだった。
- **Private行のアイコンをFilterPill/TagDropPanel/BoardMobileTagBar/拡張機能の4箇所で統一**(ドット追加+モノクロ線画アイコンに置き換え、Reicon lock/unlock、MIT)。拡張機能はv0.1.26まで用意済(zip生成済)だが**v0.1.25の審査結果が出るまで提出保留**(ユーザー判断)。
- **YouTubeサムネ「まだグレー」を根治**: `i.ytimg.com`がmaxresdefault不在の動画に「HTTP404だが中身は本物の120x90pxプレースホルダー画像」を返すため`<img onError>`が発火しない、という真因をsystematic-debuggingで特定。`isYoutubeThumbPlaceholder()`で画像サイズを見て次の画質へフォールバックする形に修正。
- **N-75完了**(SHARE画面の文言を多言語化+平易化): `share.*`名前空間に約28キーを新設、15言語対応。ARRANGE→「NEXT: LAYOUT (N)」、DONE→「BACK TO BOARD」、CREATE→「CREATE LINK」など、ユーザーとの複数ラウンドのやり取りで文言を練り直した。
- 詳細は`docs/TODO_COMPLETED.md`「セッション206」参照。全項目push・`allmarks.app`デプロイ済。

## ★次セッション最優先＝N-74/76から次を選ぶ
**ユーザー方針**: N-78(画像無しツイート専用カード)は収益化の仕組みに着手した後に回す。まずはN-74/76の2件。着手前に該当項目だけ改めてsuperpowers:brainstormingから。

| # | 内容 | 種別 |
|---|---|---|
| N-74 | SHARE画面のボードタイトル配置オプション(現状/最前面) | 視覚変更・要承認 |
| N-76 | SHARE画面の自動配置を業界標準ライブラリ活用で改善 | 技術調査から |

## 保留中(いつでも合流可)
- **N-64**: カードの＋TAGポップオーバーが、フィルタ等で一瞬消えて再表示された後にもう一度開けなくなることがある(`CardsLayer.tsx`の既存バグ)。
- **N-63**: バックアップ提案(`BackupReminder`)の表示位置がScrollMeterに被るバグ。視覚変更のためモック→承認後に実装。
- **N-65**: ECDH秘密鍵のunwrap時、生バイト列が一瞬JS側を経由する(severity LOW、設計変更要のため見送り)。
- ResizeHandleの当たり判定が小さいカードで＋TAGボタンを奪うバグ(視覚変更のため承認後)。
- TODO.mdが依然900行超(目安200行を大幅超過)→ 古いセッションnarrative(s195〜204あたり)をTODO_COMPLETED.mdへ移動する軽い掃除タスク(非ブロッキング)。
- さらなるテーマ/Flat磨き、支援まわり(非公開`docs/private/IDEAS.md`)、C2翻訳仕上げ(N-75で追加した`share.*`13言語のネイティブレビューもこれに合流できる)。

## 恒久ルール(継承)
- 視覚変更は`ui-design.md`「承認後」。`rtk`前置・`--no-verify`禁止・vitest/playwrightは素の npx(`rtk npx`は既知の不具合)・Framer Motion禁止。
- 音(dotted-notebook)/紙(paper-atelier)＝バイト同一を死守。
- 機微(支援・値付け・戦略)はtrackedに書かない＝`docs/private/`。
- merge/push/deployは必ずユーザー確認後(CLAUDE.md安全ルール)。ただしdeployは「本番で見たい」等の明示的な合図があれば即実行可。
- **選択ボックス(AskUserQuestion)はデザイン判断・意思決定・調査/デバッグ中の質問には使わない。普通の会話で聞く。yes/noの確認や「どれから着手するか」のような軽い選択も例外なく対象**(s206で2回連続で注意された、繰り返し厳守)。
- **文言(UIコピー)を新規/変更するときは、実装前に実際の英語・日本語(15言語対応時はそれも)の文面そのものを見せて確認を得る。方針・スコープの確認だけで実装に進まない**(s206のN-75で「短いボタンも含める」というスコープ確認だけで15言語書き込みまで進めてしまい指摘された教訓)。
- **IDB/vaultなど不可逆な本番データに関わる変更は、実行前に必ずユーザーに事実確認する**。
- **サブエージェントへの検証指示には、影響範囲に応じてplaywright e2eも含める**。
- **拡張機能を審査提出する前に、`_locales`フォルダ名がChromeの公式ロケールコード(地域つき、例: `zh_CN`/`pt_BR`)に沿っているか確認する**。
