# 次セッションのゴール (= セッション 40)

## ゴール

**user 指定なし** — backlog から最優先 1 件を選んで着手。 候補は順番に:

1. **multi-playback vision の board card autoplay 着手** (= 差別化機能 core、 IDEAS.md / memory `project_allmarks_vision_multiplayback.md`)
2. **テキストカード Lightbox 構造再設計** (= session 36-38 で局所 fix を重ねた領域、 まとめて作り直す機会)
3. **B-#3 重複 URL でサムネ等が出ない問題** (= 古めの未解決、 真因未調査)
4. **B-#13 TopHeader 上部 chrome brushup** (= session 39 で bottom 側は解消、 上が残)

## 開始時の動き

1. user と挨拶 + session 39 close-out 確認 (= `booklage.pages.dev` をハードリロードして、 Lightbox 開閉時に bottom-center の meter が「ぴたっと固定」 で crossfade してるか目視確認)
2. 候補 1〜4 のどれをやるか user に聞く
3. 決まったタスクの spec / plan / memory を読む
4. 提案 → 合意 → 実装 → tsc + vitest → prod deploy → user 確認

## 月末リマインダー (= 2026-05-31 約 2 週間後)

`allmarks.app` ドメイン取得確認。

## 引き継ぎ resources

- [docs/TODO.md](docs/TODO.md) — active backlog 全体
- [docs/TODO_COMPLETED.md](docs/TODO_COMPLETED.md) セッション 39 セクション — meter slot 統一 narrative
- memory `project_allmarks_vision_multiplayback.md` — multi-playback の core 仕様

## 余裕があれば backlog

上記 1〜4 のうち、 着手しなかったもの。
